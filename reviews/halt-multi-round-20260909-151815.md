# Halt artifact — code board 20260909-151815 reached the rework cap

**Chain:** Stack B Entry A, Blueprints_Home epic #25 / session #26, PR #29 (`feat/bedroom-fan-ac-coordination` → `main`).
**Artifact:** Bedroom Sleep Pre-Cool **v1.2.0** — ceiling-fan coordination + fan-only night hold, integrated onto main's v1.1.0 (manual override, helper-range clamp, daily forecast backstop) which session #19 merged and deployed at 13:17Z the same day.
**Why this file exists:** §4.5 caps the rework loop at `CYCLE=3` (derived `fix_cycles=2`). Cycle 3 returned findings, so a fourth convening is not justifiable and the board hands the artifact to the operator. Per §4.5.1 the P1/P2 findings were still actioned in-session (Task 9); what is missing is a *board* re-review of that last wave, not the fixes themselves.

## What the three cycles found

| Cycle | Bundle | Legs | Verdict | Headline |
|---|---|---|---|---|
| 1 | `5db75ba1…` (full diff vs origin/main + spec) | R1 Opus 7 · R2 gpt-6-astra 6 | FAIL | **P0**, converged + reproduced by an orchestrator render: `lock_ts` had no overnight day-shift, so from midnight to wake the fan-only lock's own off read as a person's off, `manual_off` flipped true and **the night guard was dead for the second half of every night**, with a false "Manual Override" notice each night. Plus: the settle fought the *quantised* deep-night nudge (19 vs 21 on a whole-degree unit) in an alternating command loop; a bare `turn_on` could restore `heat`; a Tuya write in flight could land after the kids-room sensor tripped. |
| 2 | `ccfe9da9…` (fix diff) | R1 Opus 8 · R2 gpt-6-astra 5 | FAIL | The P0 and the nudge loop were **closed** (verified by rendering: at 01:00 `manual_off` False, `guard_due` True; the settle now leaves 19/21/22 alone and still corrects a stale 18). But the mitigation added in cycle 1 — the "cut a fan that came on under an active sensor" step — **fired on every tick and re-cut a person's deliberate fan start every minute**, defeating the safety cutoff's own "manual override wins" contract. |
| 3 | `c831bf1f…` (fix diff) | R1 Opus 6 · R2 gpt-6-astra 4 | FAIL (cap) | The cut was now window-bounded, but two precision holes remained (armed on nights when no write is possible; a write on the last tick has no tick left to cut it), and the **heat backstop added in cycle 2 latched the night off**: its own `climate.turn_off` made `manual_off` true, killing the guard until wake. |

The pattern is worth stating plainly: both defects in cycles 2 and 3 were created by the board's own mitigations, not by the original design. Cycle 3's wave (Task 9) therefore makes both mechanisms *narrower and non-latching* rather than adding more machinery — the backstop corrects the mode instead of switching the unit off, and the cut is armed only in the windows where this blueprint can actually have issued a write, plus a 120 s delivery grace.

## State at hand-off

- All cycle-3 P1/P2 findings actioned in Task 9, commit `37ace62`; suite 350 green, deploy dry-run green. Orchestrator re-verified the two load-bearing fixes by rendering the shipped templates: the backstop's only service is now `climate.set_hvac_mode` (no `turn_off`, nothing latches), and `fans_unsafe_on` renders `[]` on a night where no write is possible while still listing a fan that came on 30 s ago under an active sensor when the night write was armed.
- **Not board-reviewed:** the Task 9 wave itself. The two prior waves each introduced a defect the next cycle caught, which is the honest reason to treat this hand-off as a real decision rather than a formality.
- TCB: `~/.claude/settings.json` changed mid-chain (operator `/model` default save + this session's permission approvals; the chain never edits that file). Declared in `~/AI/reviews/declared-tcb-e47dda9f8b1e8bcb.txt`, `verify` clean against the declaration, baseline re-baselines at merge per §7.10.

## Residuals recorded, not closed

1. **Accepted by the operator (2026-09-09).** A `fan.turn_on` reaches the Tuya cloud 10–60 s late; if the kids-room sensor trips inside that lag the fan can run at its night speed before the cut fires on the following tick, and the cut cannot restore a cutoff resume the late write cancelled. Martin chose to keep both fans in scope rather than drop the kids fan from the instance. Escape hatch: remove `fan.ceiling_fan_light_v2` from `bedroom_fans` — one instance key, no code change.
2. **Codex's standing objection.** Window membership is not command ownership: inside an armed window a deliberate start under an active sensor is still cut. Bounded by cycle 3 to the windows in which the blueprint actually writes (with the deployed instance, the 45 minutes after the bedtime lock on nights the night-fan switch is on).
3. **Unobservable stale cloud state.** If `turn_on` restores `heat`, the mode correction fails, and the cloud keeps reporting `off`, nothing in the blueprint can see it. The mode-correcting backstop bounds the nominal case, not this one.

## Operator options

- **Merge and deploy** with the residuals above accepted, using the daytime guard live-verify (beeps are free by day) before trusting a night.
- **Merge, deploy later.** Nothing forces tonight: the rooms sit below the ideal with 17 °C outside, so tonight is a cool-day no-op, and #19's own observation window is running on the currently deployed v1.1.0.
- **Hold the PR** and convene a fresh board on the Task 9 wave tomorrow, which is the one thing the cap prevented.
