# Board Review — 20260915-213915
mode: code-time | stakes: standard (trigger: blueprint logic on a live automation that actuates the bedroom AC; deploy script pinned via TCB_EXTRA, untouched) | entry: B (queued bug #30) | driver: claude | session: leviemartin/Blueprints_Home #31 (epic #18) | PR #32 | model exception: Martin approved running the chain on the Fable session (2026-09-15); reviews ran on fresh Opus 5 through the wrapper
base: main dbeda3b (v1.2.0) → branch head at review cfacc3c (plan 811ab31 + fix) → wave 1 2a60245 → post-board f561f13
legs: R1 initial = ran (claude-opus-5, high, board-reviewer, frozen `code-full.txt`, 8 findings: 1 P1 · 5 P2 · 2 P3) · R1 delta-1 = ran (claude-opus-5, high, frozen `delta-1.txt`, 3 findings: 1 P2 · 2 P3, no P0/P1) · R2 = not required (standard) · no third leg, no trial
leg_bundles: {R1: 768bf0199704ef107380dcbbc5b4ee5bef2c8c3a307aa21e259f90d9fa4a7082 (code-full.txt, KIND=diff, sentinel STACKB-BUNDLE-SENTINEL-20260915-213915), R1-delta-1: 3cd11be92069dd6c4cd38fdc7c05b20cc683847fc9669aab3917111b4e64314d (delta-1.txt, KIND=diff, sentinel STACKB-DELTA-SENTINEL-20260915-213915-1)} — both re-hashed at synthesis and equal to the preflight-authorized SHA (`.board-20260915-213915/preflight.log`)
leg_outputs: r1.json aceb03aafda443d004b2e86de9fb97fb4d3051ddadc378c085ff27198ba07b30 · r1-delta1.json 348ac69c2d1e18ac1c61874d6ef58d5faf42f1aea45a41ffdbb87cfa914c645d
finder_rounds: 1 | correction waves: 1 of 2 (delta returned to Opus) | post-board P2/P3 fixes inside the reviewed manifest, no further wave (no open P0/P1)
ledger: /home/martin/AI/reviews/precool-v121-20260915/ledger.json — total 2/24, reviews 2/3
tcb: baseline /home/martin/AI/reviews/tcb-baseline-precool-v121-20260915.txt, aggregate 8808b0066f03beb79382749e04dbc60530427f3e7c314fcbb7bca20c8552a723, verify rc 0 before dispatch (STACKB_DRIVER=claude, TCB_EXTRA=scripts/deploy-blueprint.sh), declared changes: none
probe: `.board-20260915-213915/probe-ok.txt` (LEGS=claude, standard, driver claude)
pre-board checks: 351 tests green at review (354 after the waves); RED→GREEN on the new structure test; deploy dry-run valid; origin/main unchanged since chain start; added-line scan clean (system grep); no stray files

| task | kind | provider | model / effort | outcome | duration | output |
|---|---|---|---|---|---|---|
| code-gate-31 | review | claude | requested opus / effective **claude-opus-5** / high | completed exit 0 | 149576 ms | output sha aceb03aafda443d004b2e86de9fb97fb4d3051ddadc378c085ff27198ba07b30 |
| code-gate-31-delta1 | review | claude | requested opus / effective **claude-opus-5** / high | completed exit 0 | 80071 ms | output sha 348ac69c2d1e18ac1c61874d6ef58d5faf42f1aea45a41ffdbb87cfa914c645d |

## VERDICT: PASS — open_P0: 0 | open_P1: 0 | P2: 6 (5 fixed, 1 documented) | P3: 4 (all fixed)

## Initial findings (R1) and dispositions
| id | sev | conf | lens | evidence | finding | disposition |
|---|---|---|---|---|---|---|
| R1-01 | P1 | medium | correctness | The manual-override detection (`known_setpoints`) is unchanged. | The fix gates the command site but leaves the degraded value in circulation: on a limits-unknown tick `effective_drive = max(16, 16) = 16`, so `known_setpoints` (built from `effective_drive`) contains 16 instead of the r | FIXED wave 1 (2a60245): manual_setpoint paused while the limits are unknown; scoped to PRECOOL post-board (R1-D1-01) |
| R1-02 | P2 | medium | edge | the setpoint follows on the next tick, as the 13 and 15 September starts already showed | The gate has no lower bound on how long it may suppress the setpoint: an entity that never republishes min_temp/max_temp (integration restart, permanently degraded ThinQ payload) means the AC is turned on and put in cool | FIXED wave 1: STEP 7g notice bedroom_precool_ac_limits_unknown (running spell ≥ 300 s, limits unknown); text keyed to what is measured post-board (R1-D1-02) |
| R1-03 | P2 | high | testing | conds = {s["service"]: c for c, s in precool} | Keying by service name collapses duplicate calls: if the PRECOOL phase contains more than one `climate.set_temperature` (or more than one `climate.turn_on`), only the last one is asserted on and the earlier, possibly ung | FIXED wave 1: per-service call lists, assertions over every call |
| R1-04 | P2 | high | testing | assert any("ac_limits_known" in t for t in conds["climate.set_temperature"]) | The guard is pinned by substring only, so an inverted or disjunctive gate (`{{ not ac_limits_known and ... }}`, or `{{ ac_limits_known or ((not current_setpoint_known) ...) }}`) passes this assertion unchanged; the rende | FIXED wave 1: the composed set_temperature condition rendered in five contexts |
| R1-05 | P2 | medium | edge | the PRECOOL `climate.set_temperature` call is rejected by Home Assistant ("Accepted range is 18 to 30"), aborting that tick's run | The change does more than suppress one command: because the rejection previously aborted the run, everything sequenced after the setpoint call (the fan-mode command, STEP 7 fan writes, notices, the auto-learn/bias write) | DOCUMENTED, no code change: the tick now completes (fan mode, notices, dump); pre-bedtime commands are unlimited by the beep budget; requirements updated |
| R1-06 | P3 | medium | edge | {{ state_attr(ac_climate, 'min_temp') is not none and state_attr(ac_climate, 'max_temp') is not none }} | The gate tests presence, not usability: an attribute present but non-numeric (or unparseable, e.g. 'unknown'/'unavailable' as a string) satisfies `is not none`, `/ float(16)` still degrades to the fallback, and bug #30 r | FIXED wave 1 (number test) → post-board parse-based predicate `| float(-999) != -999` on both attributes (R1-D1-03) |
| R1-07 | P2 | low | edge | 5. One optional corrective command at the deep-night checkpoint. | Only the PRECOOL setpoint is gated; the bedtime-lock setpoint write and the deep-night corrective command clamp with the same `ac_min_temp`/`ac_max_temp` fallbacks and remain ungated, so if the entity's limits are missin | ACCEPTED-DOCUMENTED: lock and deep-night writes stay ungated; their values sit inside any plausible range in the shipped configuration (requirements) |
| R1-08 | P3 | high | docs | assert "Fan-Only Night Hold (v1.2.0)" in readme | The new README bullet and the new requirements sentence are unpinned by any test (acceptance check (4) covers only the blueprint name/description and the instance alias), so either doc line can be lost in a later edit wi | FIXED wave 1: test_v121_docs_are_pinned |

## Delta-1 findings (R1) and dispositions
| id | sev | conf | lens | evidence | finding | disposition |
|---|---|---|---|---|---|---|
| R1-D1-01 | P2 | medium | correctness | manual_setpoint: "{{ ac_limits_known and ac_is_running and current_setpoint_known and not setpoint_is_known and ac_state_age_sec / float >=  | The R1-01 pause is global (STEP 2c) while the setpoint suppression it compensates for is PRECOOL-only (R1-07 keeps the bedtime-lock and deep-night writes ungated: "The bedtime-lock and deep-night setpoint writes stay ung | FIXED post-board (f561f13): pause = `(ac_limits_known or phase != 'PRECOOL')`; render rows for BEDTIME_LOCK/NIGHT_HOLD with limits unknown → detection active |
| R1-D1-02 | P3 | high | edge | and ac_state_age_sec / float >= 300 }} | The R1-02 bound is keyed to the running-spell age, not to how long the limits have been unknown — the file itself defines ac_state_age_sec as "Seconds since the unit's last STATE (hvac-mode) change". Consequences: (a) th | FIXED post-board (f561f13): message names the running spell, not an outage duration; stateless limitation (flapping restarts the age) documented in STEP 7g |
| R1-D1-03 | P3 | medium | edge | ac_limits_known: "{{ state_attr(ac_climate, 'min_temp') is number and state_attr(ac_climate, 'max_temp') is number }}" | Jinja's `number` test is stricter than the `/ float(16)` clamp it guards: an integration that publishes min_temp/max_temp as numeric strings ("18", "30.0") clamps correctly today but now reads as limits-unknown forever,  | FIXED post-board (f561f13): predicate parses like the clamps; render row for numeric strings "18.0"/"30" → known |

## Synthesis notes
- R1-01 (P1) confirmed against the artifact: `known_setpoints` is built from `effective_drive`, which equals the 16 °C fallback on a limits-unknown tick, so a blueprint-commanded 18 would fail `setpoint_is_known`. Fixed; the delta then narrowed the pause to PRECOOL because the lock and deep-night writes are not gated on the limits (R1-D1-01).
- No security finding: the change adds one read-only template variable, one conjunct, one persistent notification and no new entity writes.
- Accepted-documented: R1-07 (lock/deep-night ungated by design) and R1-05 (the tick now completes; pre-bedtime commands unlimited).
- Refutation/skeptic panel: not needed (no contested P0/P1).

## Close
- BOARD_VERDICT: PASS · OPEN_P0: 0 · OPEN_P1: 0 · waves 1/2 · reviews 2/3 · total 2/24.
- code-review-gate: suite 354 passed, dry-run valid, scan clean, no stray files, scope matches the plan; deploying work → completion stays open until [8] on #31 closes.
- Terminal: merge PR #32 (`Session: #31`, authorised by Martin 2026-09-15 "fix bug #30 and deploy it") → deploy from merged main → observe the next pre-cool start.
