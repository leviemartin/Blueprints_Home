# Board Review — 20260907-134749
- mode: design-time
- stakes: standard | trigger: default-up — automation logic change in bathroom_ventilator.yaml + new tests/instance JSON/docs; no hard trigger matched (deploy script already committed + pinned)
- artifact: spec `docs/superpowers/specs/2026-09-07-bathroom-climate-v2-design.md` + plan `docs/superpowers/plans/2026-09-07-bathroom-ventilator-v2.md` (session #11, epic #10)
- legs: R1=ran (8 findings, Claude Opus 5 `board-reviewer`) R2=ran (2 findings, Codex **gpt-5.5** — no model pin per operator policy 2026-09-05; recorded here) R3=absent-by-design (design-time) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE=off)
- quorum met: yes
- degraded: no
- base SHA: cdb6d10 (bundle) → fix wave committed on `bathroom-climate-v2` after it
- leg_bundles: {R1: d2395d3aaf6acd9b90dfe06c1ba2fe1738f10d305b70a49e6567c61d52f24b82, R2: 6557d194ece737fc42689b7b7a3b563043c1c223edbf0ad26fd8efcce4fb2778}   # canonical; sent payloads re-hashed and matched before synthesis
- main_bundle_sha: d2395d3aaf6acd9b90dfe06c1ba2fe1738f10d305b70a49e6567c61d52f24b82 (informational; R2 received the same spec+plan minus the 22 KB pytest block, its own preflighted bundle)
- finder_rounds: [10]
- synthesis_rounds: 1
- cycle: 1
- panel: {votes: none, framings: none, empirical_checks: 2 (HA docs quote on condition-step scoping; `git show 3ec393f` for the dry-run flag), inquorate: no}
- tcb_manifest_sha: 4a438dd869bcabb21e5ca110d95a40a5b33a183ac1c7fb461d2c5ba581372c91 (verify rc=0 at Phase 0c)
- call_budget: 2/24
- operator_ack: none
- probe: 2026-09-07T13:47:49Z (claude + codex reachable); both preflights `OK`, KIND=text, sentinel-asserted
- note: R2+R3 are same-family (both Codex) — their convergence counts below cross-family; the Claude-reviews-Claude self-preference residual is open until a true third family (R4) returns. R3 does not run at design-time.

## Verdict

**BOARD_VERDICT: PASS** — OPEN_P0: 0 · OPEN_P1: 0 · DEGRADED: no.

Every P0–P2 was actioned in-session before this verdict (fix wave = the two commits after `cdb6d10` on the branch). The orchestrator's empirical check on the fixed artifact: the plan's inline test suite (64 structure/render pins + 5 deploy-script checks) runs 69/69 green against the plan's inline YAML with `-W error::DeprecationWarning`, and HA's live `/api/template` renders the new `notify_list` filter and the `sensors_lost_minutes` arithmetic as the harness does. Design-time has no executable check for the design itself; adjudication below is by interface/invariant analysis plus two documented checks.

## Findings (sorted P0 → P3; converged before singleton)

| id | sev | conf | lens | source | section | finding (evidence) | refutation_status | disposition |
|---|---|---|---|---|---|---|---|---|
| B20260907-134749-01 | P0→P3 | high | correctness | R1-01 | plan T2 STEP 3 | Bare `- condition: template` in the mold branch "aborts the run" when false (`{{ minutes_since_fan_change \| float >= 60 }}`) | **refuted as stated** — HA docs (scripts → "Test a condition"): "When it is used inside a choose action, only the actions within that choose will stop." The gate could only skip the push, not STEP 4–6. | FIXED anyway: the gate and its variable were deleted per -02; pin `test_no_bare_condition_steps_anywhere` added so no bare condition step can reappear in any sequence. |
| B20260907-134749-02 | P2 | high | logic | R1-02 | spec §3.5 vs §3.4 | The hour-gate's premise ("re-enters every max-run cycle") is false: rule 3 `mold_on` outranks rule 5 `max_run`, so the edge fires once per mold episode | none | FIXED: gate + `minutes_since_fan_change` removed; spec §3.5 and the YAML comment rewritten to state the once-per-episode invariant. |
| B20260907-134749-03 | P2 | high | logic | R1-03 | spec §3.5/§3.2, plan Phase 0 criterion 5 | Sensor warning was edge-triggered only, so the outage already live at deploy (Aqara offline since 08-01) would never be announced; a failed `sensors_back` gate could leave the warning stuck | none | FIXED: STEP 2a is state-driven (create while `not sensors_ok and sensors_lost_minutes ≥ grace`, dismiss while `sensors_ok`, both `continue_on_error`); STEP 2b keeps the pushes edge-gated. Pins `test_sensor_warning_is_state_driven` + `test_sensor_push_branches_are_edge_triggered`. |
| B20260907-134749-04 | P2 | high | edge | R2-002 | plan T2 STEP 1 | Degraded decision ignored `sensor_grace_min` (`{% elif not sensors_ok %}degraded`): a 1-min blip flips the fan immediately | none | FIXED: `sensors_lost_minutes` (from the failing sensor's `last_changed`); rule 2 requires ≥ grace, new rule 2b `hold` keeps `fan_is_on` inside the grace window. Pins `test_rule_hold_within_grace`, `test_rule_degraded_after_grace`; spec §3.4/§3.6 updated. |
| B20260907-134749-05 | P2 | high | boundary | R2-001 | plan T2 STEP 1 | `notify_targets` is free text dispatched as a service name — any typed service would be called with title/message data | none | FIXED: `notify_list` filters to `^notify[.][a-z0-9_]+$`; every push `for_each` iterates it (pinned); render pin `test_notify_list_filters_non_notify_services`; spec §3.1 documents the filter. |
| B20260907-134749-06 | P2 | high | completeness | R1-04 | spec §6 vs plan T1–T4 | Spec §6 requires a deploy-script test; no task created it | none | FIXED: T1 step 0 creates `tests/test_deploy_blueprint_script.py` (syntax + 4 dry-run cases); Files block and commit updated. |
| B20260907-134749-07 | P2→P3 | medium | interface | R1-05 | plan T2 s4 / T3 s2 vs spec §5 | If the pinned script lacked `--dry-run`, adding it would be an undeclared TCB change | **refuted** — `git show 3ec393f:scripts/deploy-blueprint.sh` contains the flag (4 hits) and the dry-run was exercised on both the v1 and v2 instance shapes this session | Doc fix applied: spec §5 now declares the `--dry-run` mode in the CLI contract. No script edit; `declared_tcb_changes` stays empty. |
| B20260907-134749-08 | P3 | high | logic | R1-06 | spec §3.6 | Manual-plug row overstated the hold (rule 5 cap) and equated boost with it | none | FIXED: row rewritten (manual ON held ~`min_runtime` at normal RH, longer only via rule 7; boost holds its full runtime). |
| B20260907-134749-09 | P3 | high | conventions | R1-07 | plan T2 STEP 4 | Fan calls and notification creates lacked `continue_on_error` (repo convention pinned in the LG AC suite) | none | FIXED: added to `homeassistant.turn_on/off` and every `persistent_notification.create`; pinned in `test_fan_call_idempotent` and the notification tests. |
| B20260907-134749-10 | P3 | high | consistency | R1-08 | plan T1 Interfaces vs T2 | Variable contract between tasks was incomplete | none | FIXED: the removed variable is gone; `sensors_lost_minutes` and `notify_list` added to the Produces list in file order. |

Nits: 0 of cap 5. Dismissal memory: no `feedback_board_dismissed_*` entry matched.

### Orchestrator pre-board findings (triple-check passes 0–4, fixed before dispatch, listed for the record)
- P1 test harness time base (night tests) — fixed (`at()`/`render_at()`).
- P1 helper creation used a non-existent REST endpoint — fixed (WS `input_boolean/create`, live-probed).
- P2 deploy backups gitignore moved to T3; P2 fetch-origin guard + classifier contingency added to T4.
- P2 mold push hour-gate — added pre-board, then **removed** by this board (finding -02): the orchestrator's premise was wrong.

## Residual risks (accepted, documented in spec §3.6)
- HA restart resets Hue `last_changed` (fresh run budget, presence reads recent for one window) — bounded.
- Recovery push needs both sensors numeric; temperature-only late return ends degraded mode silently (same device).
- `sensors_lost` `for` with a two-state `to` list may restart its timer on unavailable↔unknown flips — only lengthens the grace.

## Terminal return
BOARD_VERDICT: PASS · OPEN_P0: 0 · OPEN_P1: 0 · REPORT: reviews/board-20260907-134749.md · DEGRADED: no
