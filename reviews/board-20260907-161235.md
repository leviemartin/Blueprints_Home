# Board Review — 20260907-161235
- mode: design-time
- stakes: standard | trigger: default-up — automation logic change in bathroom_heating_rack.yaml + new tests/instance JSON/docs; no hard trigger matched (deploy script pinned via TCB_EXTRA, not edited)
- artifact: spec `docs/superpowers/specs/2026-09-07-bathroom-climate-v2-design.md` (§4) + plan `docs/superpowers/plans/2026-09-07-bathroom-heating-rack-v2.md` (session #13, epic #10, Entry B)
- legs: R1=ran (9 findings, Claude Opus 5 `board-reviewer`, dispatched against the frozen payload file with a mandatory presence check; 177k tokens, 15 tool uses) R2=ran (6 findings, Codex **gpt-6-astra** — no model pin per operator policy 2026-09-05; codex-cli 0.153.4, `reasoning effort: none` reported by the CLI) R3=absent-by-design (design-time) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE unset → off)
- quorum met: yes
- degraded: no
- base SHA: a25108a
- leg_bundles: {R1: 5228380bfaa4f961e4f361418ca453e17b273bda09debcc50d669e4bbfd0a135, R2: 793219dfd65786506cfb87d0c3a39c1365ea8cb19b0d25e766b49909a4726b3a}   # canonical (§5.2); R2 = spec minus §3 + plan minus the pytest/doc blocks
- main_bundle_sha: 5228380bfaa4f961e4f361418ca453e17b273bda09debcc50d669e4bbfd0a135 (informational)
- finder_rounds: [15]
- synthesis_rounds: 1
- cycle: 1 (fix_cycles = 0)
- panel: {findings: 1 (R2-003), votes: evidence(Opus)=stand · defense(Opus)=refute · Codex gpt-6-astra=refute → KILL (cross-family among the refuters, 2/3 → confidence medium), framings: evidence-chain + defense + cross-family defense, empirical_checks: 3 (continue_on_error vs missing action on a throwaway HA script; live /api/template renders of notify_list / idle rounding / floor arithmetic; pytest 116/116 + validate_config on the fixed inline artifacts), inquorate: no}
- tcb_manifest_sha: 94cccc64bc85efa5b38309bca448ff5dd1591f7c7598391ebd0a6e8a9b66caa7 (verify rc=0 at Phase 0c)
- call_budget: 5/24 (R1, R2, 3 skeptics)
- operator_ack: none
- probe: 2026-09-07T16:14:22Z (claude + codex reachable); both preflights OK, KIND=text, SENTINEL=floor_hysteresis
- note: R2+R3 are same-family (both Codex) — their convergence counts below cross-family; the Claude-reviews-Claude self-preference residual is open until a true third family (R4) returns. R3 does not run at design-time.
- integrity: dispatched payloads re-hashed before synthesis — R1 `5228380b…` match, R2 `793219df…` match, panel packet `31bb9b51…` match (preflight OK, KIND=packet, SENTINEL=R2-003).
- fix wave: plan `9d35391` + spec `1a1ab50` on `bathroom-heating-rack-v2`; the plan's inline artifacts were re-scratch-run after the wave (rack `PASS=116 FAIL=0`, RED vs v1.1.1 `PASS=20 FAIL=96`, suite `PASS=249 FAIL=0`, deploy dry-run OK, HA `validate_config` valid).

## Verdict

**BOARD_VERDICT: PASS** — OPEN_P0: 0 · OPEN_P1: 0 · DEGRADED: no.

Every P0–P2 was actioned in-session before this verdict except two P2s down-ranked to P3 with a recorded interface rationale (B-10, B-11). The one P1 (R2-003) was contested and went to the skeptic panel; it died under the §5.3 kill rule (Codex refuter + one Opus refuter), and its subject is covered by the actioned sensor-loss policy (B-06). Design-time has no executable check for the design itself; the three empirical checks above pin platform facts (HA `continue_on_error` semantics, template rendering, schema validity), not the design.

## Findings (sorted P0 → P3; converged before singleton; cross-family convergence first)

| id | sev | conf | lens | source | section | finding (evidence) | refutation_status | disposition |
|---|---|---|---|---|---|---|---|---|
| B20260907-161235-01 | P2 | high | security/conventions | converged(R1-05, R2-001) — cross-family | plan T2 STEP 3/7 push fan-out; spec §3.1 vs §4 | `for_each: "{{ notify_targets }}"` — free-text entries dispatched as arbitrary service names; the ventilator already filters to `^notify[.][a-z0-9_]+$` | none | **FIXED** — `notify_list` filter in STEP 1, all three fan-outs iterate it; pins `test_notify_list_filters_names`, fan-out pin asserts `notify_targets` never dispatched |
| B20260907-161235-02 | P2 | high | correctness/boundary | converged(R1-03, R2-006) — cross-family | plan T2 STEP 1 + STEP 7; `idle_setpoint` selector `{min: 5 …}` | edges and deadband compared the RAW `idle_setpoint` while the device holds the rounded/clamped value (7.5→8.0; 5 → rejected by HA, aborting the run before boost cleanup) | none | **FIXED** — `setpoint_min/max`, `idle_setpoint_dev` (rounded + clamped) used in both edges; `desired_setpoint` clamped to the device range; rows `test_idle_setpoint_dev_rows`, `test_setpoint_clamped_to_device_range`, `test_warmup_edges_use_the_device_idle_value` |
| B20260907-161235-03 | P2 | high | correctness | R1-01 | plan T2 STEP 2 `ma_auto_start_dt` | the ΔT lead moves ±1.5 min with the sensor's ±0.3 °C swings, re-opening/closing the window across the 1-minute tick at the opening edge (newly exposed by `warmup_min_minutes` 28 → 10) | none | **FIXED** — `<p>_open_dt` = `target_warm − warmup_max` while `<p>_heating`, else the ΔT `auto_start`; `test_opening_edge_latches_while_heating` |
| B20260907-161235-04 | P2 | high | logic | R1-02 | spec §4.1 comfort floor; plan T2 STEP 1 `floor_hysteresis` | deadband keyed on "any setpoint above idle" widened every slot's floor under boost, vacation leftovers or a manual setpoint | none | **FIXED** — per-slot `<p>_target_dev` / `<p>_heating` (device setpoint == this slot's rounded target); `test_deadband_is_keyed_to_the_slot_not_any_raised_setpoint` |
| B20260907-161235-05 | P2 | high | correctness | R1-04 | plan T3 requirements; spec §4.1 Push; T2 STEP 3 ordering | "`continue_on_error` … one bad target never halts the run" is false for a missing action; the sensor-warning fan-out preceded the climate calls | **confirmed by empirical check** (throwaway script: `continue_on_error: true` on `notify.does_not_exist_board_probe` → `script_execution: error`, "Action … not found", next step never ran) | **FIXED** — every push after the climate calls; docs/spec claim corrected; T4 step 2 checks each target exists in `/api/services` before deploying |
| B20260907-161235-06 | P2 | medium | logic | R1-06 | spec §4.1; plan T2 STEP 1/2 | the fallback source (rack's own sensor, ~1.4 °C warm) would silently satisfy every slot when the Hue sensor dies; warning only on both-unavailable | none | **FIXED** — floor suspended while `indoor_temp_has_primary` is false (slots heat on schedule, device thermostat bounds); state-driven `heating_rack_sensor_warning` naming the fallback; `test_floor_suspended_without_room_sensor` |
| B20260907-161235-07 | P2 | high | availability | R2-004 | plan T2 STEP 3 (climate/sensor branches on the `/1` tick) | outage pushes repeated every minute of an outage (~1,440/day/target) | none | **FIXED** — `climate_lost` (5 min) / `temp_lost` (10 min) state triggers; pushes nested under `trigger.id == …`; in-HA warnings state-driven with dismiss on recovery; `test_outage_pushes_are_edge_gated` |
| B20260907-161235-08 | P2 | high | consistency | R1-07 | plan Phase 0 observation criteria 3/4 | "exactly one push" unevaluable: today's room sits at the floor line (expected: zero pushes), and a fan pause/resume legitimately pushes twice | none | **FIXED** — criteria restated as two trace-decided branches (a)/(b) |
| B20260907-161235-09 | ~~P1~~ killed | medium | failure | R2-003 | plan T2 STEP 1/3 | both temperature sources failing → synthetic 20 °C authorises scheduled heating for the window | **panel: KILLED** — evidence-chain (Opus) `refuted:false` (chain intact); defense (Opus) `refuted:true` (state untriggerable on this hardware per April session notes; v1 has no temperature gate, so no regression; the proposed inhibit is the fail-silent guard behind the five-week ventilator incident); Codex gpt-6-astra `refuted:true` (climate-unavailable branch stops the run before any command; Hue loss alone never selects 20 °C) | subject covered by B-06's explicit sensor-loss policy (heat on schedule + warning); recorded in the plan's behaviour block |
| B20260907-161235-10 | P2→P3 | medium | restart | R2-002 | plan T2 STEP 1 `boost_age_min` | HA restart restores the helper with a fresh `last_changed`, re-arming the boost interval once | orchestrator interface analysis: pre-existing (v1 identical), bounded by `boost_runtime_min` per restart; a persisted expiry needs a new helper — out of scope, no new inputs | **ACCEPTED, documented** (plan behaviour block) |
| B20260907-161235-11 | P2→P3 | medium | latency | R2-005 | plan "Behaviour stated for the board" | no bound on repeated set_temperature + push while cloud telemetry lags | orchestrator interface analysis: Tuya cloud pushes the attribute within seconds on this device (09-07 history); the repeated call is idempotent; a pending-command tracker is over-engineering for a blueprint | **ACCEPTED, documented** |
| B20260907-161235-12 | P3 | high | consistency | R1-08 | plan T2 STEP 8 debug vs Phase 0 criterion 2 | Morning/Evening B debug lines lacked `hold_until=` | none | **FIXED** — all four lines render `open=`, `hold_until=`, `heating=`, `floor=` |
| B20260907-161235-13 | P3 | medium | correctness | R1-09 | plan T2 STEP 6 vs STEP 7 | boost `turn_off` restart aborted the dismiss/notification steps of that run | none | **FIXED** — boost expiry is the last action step; `test_boost_expiry_is_the_last_step` |

Noise control: 13 findings after cross-leg dedupe (2 cross-family convergences), 0 nits, none below the confidence floor, none matched a `feedback_board_dismissed_*` entry. Scope: all findings sit inside the reviewed spec §4 + plan.

## Empirical checks (orchestrator-run, recorded)

1. **HA `continue_on_error` vs missing action** — throwaway `script.board_coe_probe_20260907` (`persistent_notification.dismiss` → `notify.does_not_exist_board_probe` with `continue_on_error: true` → `persistent_notification.create`): run rc 400, trace `script_execution: error`, `last_step: sequence/1`, log "Service not found … Action notify.does_not_exist_board_probe not found"; the create step never ran; script deleted, notification dismissed. RED for the docs claim → design changed (B-05).
2. **Live template render** (`POST /api/template`): `notify_list` filter → `['notify.mobile_app_martin_fold']`; idle rounding 7.5/1.0 → `8.0`; floor `(24.0 − 1.0 + 0.5) | round(2)` → `23.5` — identical to the pytest harness.
3. **Fixed inline artifacts**: rack suite 116/116, RED 96/20 vs v1.1.1, full suite 249/249, deploy dry-run OK, HA `validate_config` on the input-substituted config `triggers valid, actions valid`.

## Skeptic panel — R2-003

| lane | model | vote | basis |
|---|---|---|---|
| evidence-chain | Claude Opus 5 (`skeptic-evidence`) | `refuted: false` | chain intact (quote exists, entails, the plan's own test constructs the state); likelihood is not its lane |
| defense | Claude Opus 5 (`skeptic-defense`) | `refuted: true` | state untriggerable on this hardware (April notes), no regression vs v1 (no temperature gate), inhibit = the fail-silent guard behind the ventilator incident |
| cross-family | Codex gpt-6-astra | `refuted: true` | climate-unavailable branch stops before any command; Hue loss alone never selects 20 °C; P1 not established |

Kill rule §5.3: refuters include the cross-family skeptic → **killed**; split 2/1 → confidence medium. Not a security finding.

Report generated 2026-09-07T16:44:57Z.
