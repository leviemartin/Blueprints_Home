# Board Review — 20260818-194701
mode: design-time | stakes: standard | bundle: 966e5436fc989257316d13da9f38d0aff2159d20922b3ee22d1b7aab37b7626b | base: 69fbeb9
legs dispatched: R1 (Claude Opus), R2 (Codex gpt-5.4); R3 (Gemini) UNAVAILABLE — standing
quorum: pending | degraded: yes (architecture lens absent)

ℹ DEGRADED BOARD: architecture lens (Gemini R3) absent — currently unavailable on this machine.

## Round 1 — bundle 966e5436… (spec+plan as first drafted)
Legs: R1 ran (BUNDLE-SHA-SEEN matched preflight — integrity OK), R2 ran (exit 0, 4 findings). Quorum met.

| id | sev | conf | lens | source | finding | status after fix wave |
|---|---|---|---|---|---|---|
| B194701-01 | P1 | high | correctness/edge | converged(R1-3,R2-1,R2-4) | Device-limit clamp collapses active setpoint onto the release threshold → silently reinstates the unreachable-off defect; test plan pinned the collapsed value as expected | FIXED: C1 redesign — device-only clamps + strict post-clamp feasibility gates (`cool_deep_ok`/`heat_deep_ok`) + fallback to boundary; invariant-level gate pins replace the numeric collapse pin |
| B194701-02 | P2 | high | correctness | R1-1 (orchestrator-verified by arithmetic) | temp_low/temp_high clamps (user inputs, 0.5-grid) can emit non-step-aligned commands on step-1.0 devices → idempotence guard never closes → command+beep every 10-min tick; false manual holds | FIXED: device-only clamps; command grid-aligned by construction; alignment edge row (th 23.5/tl 21.5/step 1.0 → 22.0) pinned |
| B194701-03 | P2 | high | logic | R1-2 (orchestrator-verified) | Clamped command can sit exactly on the opposite mode's trigger line (cool→temp_low / heat→temp_high) → full-comfort-range hunting on coarse-step devices | FIXED: strict `> temp_low` / `< temp_high` terms in the feasibility gates; infeasible → boundary fallback |
| B194701-04 | P2 | high | logic | converged(R1-5,R2-2) | Hold expected-state advances on command dispatch, not observed convergence; dropped LG cloud command → false 60-min hold; v1.2.0 raises trigger frequency | DEFERRED with reason (spec residual risk 6): feature inert — hold helpers do not exist (live-verified); hard gate recorded — convergence-gated commit (v1.2.1) REQUIRED before enabling helpers. Operator visibility: yes |
| B194701-05 | P2 | high | logic | R1-4 | comfort_margin default 0.5→1.0 + C4 gate hard-stops any narrow-range (<2.0 °C) sibling instance including its safety pierces | MITIGATED: instance blast-radius checklist in spec Migration + plan T5 (re-enumerated at deploy time); live 2026-08-18: single instance, width 2.0, legal |
| B194701-06 | P3 | high | logic | converged(R2-3,R1-9) | Regime-transition discontinuity: deadband flip mid-mode forces a deep-pull inside the band; weather-outage disjunct flips regimes instantly | RULED INTENDED with rationale (residual risk 5): pull bounded by the external release and ends in OFF (operator's stated preference); outage direction consistent with v1.1.0 fail-safe. Surfaced for operator confirmation |
| B194701-07 | P3 | high | spec-consistency | R1-6,R1-7 | Description/message/doc changes unpinned; two edge rows weak (non-binding tie; validation-illegal params) | FIXED: v1.2.0-only description token + C4 message pin; edge table rebuilt — 9 rows, all validation-legal, strictly binding |
| B194701-08 | P3 | high | convention | R1-8 | STEP-1 insertion point unspecified; too-early placement = Undefined at runtime, invisible to get_var/render_var tests | FIXED: C1 names the insertion point; ordering index test added |
| B194701-09 | P3 | high | correctness | R1-10 | T5 deploy gate branch-dependent → false red / hand-waved pass | FIXED: gate re-based on `changed_variables` containing the v1.2.0-only variables |

Nits: 0 of cap 5. Auto-refutations: none (no deterministic pre-check contradicted a finding; suite has no coverage of the new surface yet — RED by design).

## Round 2 — bundle 797279da… (fix-wave verification)
Legs: R1 ran (BUNDLE-SHA-SEEN matched — integrity OK; 8 findings), R2 ran (exit 0; 3 findings). Quorum met. All round-1 fixes verified arithmetically correct on the then-current 9-row table by both legs; round 2's yield was fix-introduced defects — consistent with this repo's board history (third consecutive chain where the fix-wave round caught the highest-severity issue).

| id | sev | conf | lens | source | finding | disposition |
|---|---|---|---|---|---|---|
| B194701-10 | P1 | high | correctness | converged(R1R2-1,R2R2-1) | Gate-false fallback paired with the doubled default margin idles DEEPER than v1.1.0 ("fallback to v1.1.0" claim false); infeasible configs get no operator signal; per-instance device attrs absent from deploy checklist | FIXED (rev 3): default stays 0.5, operator instance set to 1.0 explicitly (C3); C7 self-healing infeasibility notification; checklist now live-reads device attrs + computes predicted gates per instance, deploy gate fails on a False prediction. Sub-claim "step 0.5 asserted, not probed" REFUTED with session evidence: target_temp_step 0.5 live-read from climate.livingroom states this session (refutation: code-path/live-probe — admissible) |
| B194701-11 | P2 | high | correctness | converged(R1R2-3,R2R2-2; orchestrator-reproduced numerically) | Binary-float dust skips the heat extra-step on 0.1-grids → setpoint collapses onto the release, gate False, feature silently inert (all-heat, all-step-0.1; never unsafe direction) | FIXED (rev 3): epsilon grid-count quantization (±0.001 in grid units) + round(2) on both sides of every gate compare; dust rows 14–16 pinned |
| B194701-12 | P2 | high | logic | R1R2-2 | Minimal-depth quantization can shrink authority to 0.1 °C with gate True → practical stall despite "feasible" | FIXED (rev 3): `deep_pull_depth = max(ac_temp_step, 0.5)` (bias floor from live measurement) enforced in BOTH the quantization target and the post-clamp gate (catches clamp-thinning, row 8); residual risk 1 rewritten to state heat's real exposure |
| B194701-13 | P2 | high | correctness | converged(R1R2-4,R2R2-3) | Edge table structurally blind to both fix-introduced defect classes (only binary-exact margins/steps) | FIXED (rev 3): table expanded to 16 rows incl. 0.1-step dust rows, thin-depth, clamp-thinning; all expected values verified by exact-arithmetic simulation |
| B194701-14 | P2/P3 | high | spec-consistency | R1R2-5,R1R2-6 | Decision §, C4 rationale, and residual risk 1 still described the superseded round-1 algorithm | FIXED (rev 3): Decision states the implemented rule; C4 straddle sentence corrected; risk 1 rewritten |
| B194701-15 | P3 | high | correctness | R1R2-7 | Ordering pin didn't cover gate-after-its-setpoint dependency (Undefined kills the whole tick incl. pierces) | FIXED: full ordering chain pinned (plan T1.9) |
| B194701-16 | P3 | med | correctness | R1R2-8 | "Storable by definition" overclaims grid-validity for non-anchored device grids | FIXED: claim softened to LG-observed anchored grids; re-anchoring at ac_min declined (pre-existing v1.1.0 property, YAGNI) |

## Synthesis — final
- Rounds: 2 (cap reached). Deterministic pre-checks: baseline suite 53/53 green on v1.1.0; new surface RED-by-design until execution.
- Auto-refutations: none. Refutations with evidence: 1 (B194701-10 sub-claim, live-probe).
- Open P0: 0. Open P1: 0. Deferred-with-reason: B194701-04 (hold convergence-commit — feature inert, hard gate on helper creation, v1.2.1). Operator-confirmation items: B194701-06 (regime-transition pull ruled intended).
- Nits: 0/5. Dismissal memory: no matching feedback_board_dismissed_* entries.

BOARD_VERDICT: PASS (at round cap 2)
OPEN_P0: 0 | OPEN_P1: 0
DEGRADED: yes — architecture lens (Gemini R3) absent, standing. Quorum Claude+Codex met both rounds.
REPORT: reviews/board-20260818-194701.md
