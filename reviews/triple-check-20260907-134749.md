# Triple-Check — spec 2026-09-07-bathroom-climate-v2-design.md + plan 2026-09-07-bathroom-ventilator-v2.md (session #11)

**Posture:** Skeptical. Default verdict: not yet. Burden of proof on the proposal.

## 0. Premise verdict

- **Should this exist?** Yes.
- **Reasoning:** the live automation has been inert since 2026-08-01 and its detector is dead by construction; the research verdict is *proceed* on three verified T1-backed directions.
- **Alternatives considered:** derivative/trend helper vs from/to jump (jump chosen: Aqara reports on a 6% delta, no extra helper); reference-sensor detection (rejected — abandoned by the most-adopted blueprint's maintainer); keeping the timer purge (rejected — not a recognised mode in ASHRAE 62.2 / Bbl / ADF once supply vents exist); delay-based min-run (v1) vs stateless `last_changed` timing (stateless chosen so boost and sensor loss can interrupt).

## 1. Findings

### P0 — must address before execution
- Board B20260907-134749-01 (R1): bare condition step in the mold branch. **Refuted as stated** by the HA docs (a failed condition inside a `choose` stops only that choose) — but the site was deleted anyway (see -02) and a pin now forbids bare condition steps in any sequence. Closed.

### P1 — significant gap
- Test harness time base for tests that override `now` — found by the orchestrator's scratch run; fixed with `at()`/`render_at()`. Closed.
- Helper creation via a non-existent REST endpoint — found by the live probe (404); fixed to WS `input_boolean/create`. Closed.

### P2 — clear bug or omission
- Board -02 (mold push gate premise wrong) → gate removed. Closed.
- Board -03 (sensor warning must be state-driven for the outage that is live at deploy) → STEP 2a/2b split. Closed.
- Board -04 (degraded mode must honour the grace window) → `sensors_lost_minutes` + hold rule. Closed.
- Board -05 (notify targets must be filtered to `notify.*`) → `notify_list`. Closed.
- Board -06 (deploy-script test missing from the plan) → T1 step 0. Closed.
- Board -07 (dry-run flag undeclared in spec §5) → refuted as a blocker (flag exists at the pinned commit), spec text fixed. Closed.
- Orchestrator: deploy-backup gitignore moved into T3; fetch-origin guard + classifier contingency in T4. Closed.

### P3 — polish / future-proofing
- Board -08/-09/-10 (manual-plug row, `continue_on_error` convention, task interface list) → all fixed. Closed.
- Open, accepted: `sensors_lost` `for` with a two-state `to` list may restart its timer on unavailable↔unknown flips (only lengthens the grace); recovery push needs both sensors numeric (same device); HA restart resets Hue `last_changed` (bounded). Documented in spec §3.6.

## 2. Adjacent areas requiring coordinated change

- **Downstream consumers:** heating rack v1.1.1 T6 watches `light.heater` (Hue room group mirroring the plug) — unchanged by this session; session 2 keeps the coordination. README + requirements rewritten in T3.
- **Upstream callers:** Aqara T1 (offline; entity ids may change on re-pair → T4 step 2), Hue motion, Met.no primary / OWM fallback — read-only inputs.
- **Memory entries to prune/update:** `project_bathroom_ventilator_blueprint_20260907` (chain log at close); `reference_ha_ops_cluster` (add: helpers have no REST config endpoint — use WS `<domain>/create`; `gh repo view` rejects a path — already filed).
- **In-flight worktrees / specs touching this surface:** none (`.stack-b` re-pointed to epic #10; LG AC chain closed).
- **Tests required:** 64 structure/render pins + 5 deploy-script checks (inline in the plan, verified 69/69 in a scratch copy); live confirmation = T4 debug dump + first-shower trace.
- **Convention parity:** pytest structure pins (LG AC style) ✔ · per-pillar commits ✔ · `!input` pass-through variables ✔ · notify fan-out cloned from the rack ✔ · `continue_on_error` on cloud calls ✔ (after board -09) · deploy script keeps `set -euo pipefail` (deliberate, stated in the plan).

## 3. Past failure cross-check

- **Memory entries reviewed:** 290 `feedback_*` descriptions (60 triaged as adjacent).
- **Directly relevant (mitigated):** blueprint-input-rename → instance migration + offline key validation; HA bare-boolean text → all booleans are `{{ }}` expressions (pinned); HA history one-day default → probes used `end_time`; HA config round-trip poisoning → hand-authored instance JSON; fetch-origin-before-deploy → T4 step 1; classifier blocks prod deploys → §4.6 contingency in T4; verify-script-behaviour-not-path → dry-run exercised, live enabled-assert in T4; dormant RED floors / realized property → observation criteria demand a real shower trace; choose-default catch-all with new triggers → no `default:` branches; role-based helpers → blueprint inputs are the roles; Codex model pin retired + argv/stream limits → R2 dispatched unpinned (gpt-5.5) via stdin on a 64 KB bundle; probe-ok 3600 s → legs finished within the window.
- **Plausibly adjacent (no action):** Jinja grid-float compares; Sentry no-op in bare scripts; worktree glob pollution.

## 4. Action plan

All P0/P1/P2 findings: **fixed now** (commits on `bathroom-climate-v2` after `cdb6d10`, verified by the 69/69 scratch run and two live HA template renders). Nothing deferred to a backlog. Accepted-as-is items are the three documented residuals above.

## Passes 2.5 and 4.5
- **Live API probe:** done (input_boolean WS list/create schema, services list, weather attributes, plug state shape, `/api/template` renders) — one plan correction resulted.
- **Board:** `reviews/board-20260907-134749.md` — PASS, OPEN_P0 0, OPEN_P1 0, not degraded.

**Terminal state: execute** (subagent-driven: T1–T3 to Sonnet, T4 in the main loop after the code-time board and merge).
