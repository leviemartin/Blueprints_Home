# Triple-Check — climate follow-up: spec `2026-09-09-climate-followup-v1.1.0-design.md` + plan `2026-09-09-climate-followup-v1.1.0.md` (Blueprints_Home #19, design-time gate [4])

**Posture:** Skeptical. Default verdict: not yet. Burden of proof on the proposal.
**Board:** 20260909-113000 (Pass 4.5 — findings merged in §5 below when the legs return).

## 0. Premise verdict

- **Should this exist?** Yes.
- **Reasoning:** Every item is a locked operator decision on #19 with a concrete defect or gap behind it (the rejected auto-learn write on 2026-09-08, dead `forecast`-attribute code, a PRECOOL loop that re-asserts over any human change within a minute, a living-room unit that runs its full window with nobody home). Research-validate returned **proceed** with the noted uncertainties carried into the spec as explicit choices.
- **Alternatives considered (spec §7):** hourly fetch on every tick instead of a daily backstop (outside the locked R1-07 wording and the #19 acceptance criterion); Context-based override detection (blind for time-pattern automations — verified live); expected-state helper (design B) as the fallback; a separate guest-mode input (folded into `home_indicators`); `alarm_control_panel.alarmo` as an away confirmation (not wired, YAGNI).

## Pass 0 — Research-validation re-check

`docs/superpowers/research/2026-09-09-climate-followup-research-validation.md` present; verdict proceed retained. Anti-patterns flagged (5) each considered: off-for-short-absences (locked setback, not off); context-based detection (rejected, design C); `state_attr(weather,'forecast')` (deleted); unsupported `get_forecasts` type without `continue_on_error` (gated on feature bit 1 + `continue_on_error: true`); re-asserting every tick with no override path (the override item). No contradiction.

## 1. Findings (single-model passes 1–4)

### P0 — must address before execution
None found, having looked for: data loss on the helper write (the clamp removes the rejection path), unbounded beeps (every new command sits behind an existing idempotency guard or the override gate), an automation left `unavailable` by an input rename (all new inputs additive; the LG rename is variable-level), a manual override that could strand the unit in heat/cool overnight (NIGHT_HOLD/DEEP hold semantics unchanged), and a deploy that could brick an instance (dry-run + HA `validate_config` both green; deploy script unchanged).

### P1 — significant gap
1. **Known-setpoint set compared computed values, not device-held values** — `known_setpoints` (plan diff, STEP 2c) listed `maintaining_setpoint ± correction_step` unquantised. The core `lg_thinq` climate code sends `int(temperature)` when `target_temp_step >= 1`, so a whole-degree unit holds 21 for a commanded 21.5 and every tick would read as manual (memory: compare against the value the device actually holds; Jinja grid quantisation). **Fixed pre-board (commit 434069d):** `ac_temp_step` discovered in STEP 2a; each known value is `int(v)` when the step is ≥ 1; test `test_rendered_known_setpoints_are_the_values_the_device_holds`.

### P2 — clear bug or omission
1. **Cloud-blip manual-off false positive** — a ThinQ outage that ends inside `[bedtime − lead_cap, lock)` re-creates the `off` state with a fresh `last_changed`, indistinguishable from a person switching the unit off; that night reads as a manual off. No stateless signal separates the two (no history access in templates). **Accepted-documented:** the notice names the recovery (switch the unit on by hand — a running unit inside the window is adopted as PRECOOL at once); requirements doc + spec §3.2 residual (4).
2. **Plan RED count measured on the wrong state** — Task 1 applies the tests AND the instance JSON together, so `test_precool_instance_values` is already green there; the expected count is 32 failed / 67 passed, not 32/66. **Fixed** (re-measured on the exact T1 state).
3. **Adjacency: sessions #22 and #24 are observing the live v1.0.3** — deploying v1.1.0 ends their windows. **Fixed:** Task 5 posts a note on both issues at deploy time (spec §6.5).

### P3 — polish / future-proofing
1. A helper whose `min` exceeds 120 (absurd config) makes `bias_floor > bias_ceiling`; the write then lands at 120 and is rejected by the helper. The STEP 7d notice already names the helper's range on every tick; no code change.
2. Pre-existing, out of scope: the PRECOOL/lock idempotency guards compare against the unquantised desired setpoint (`|current − desired| > 0.1`), so a whole-degree unit with a half-degree maintaining setpoint would re-send every minute — a v1.0.x property of the live blueprint on a device the household does not own. Recorded for a later session.
3. `README.md` has no "LG AC Climate Control" section (only "LG AC Sleep & Movie Mode"); the LG v1.3.0 feature bullet has nowhere to land — requirements doc carries it. Not added (no section = no drift).

## 2. Adjacent areas requiring coordinated change

- **Downstream consumers:** `deploy/*.json` (migrated, T1), `requirements_*.md` + `README.md` (T4), the two test files (T1). `docs/superpowers/plans/2026-09-08-bedroom-precool-v1.0.3-night-fan.md` is Done and stays as history.
- **Upstream callers:** HA entities — `input_number.autolearner` (range verified live, pre-checked at deploy), `weather.home_sm` (daily+hourly verified live), `person.*` / `input_boolean.security_*` (ids verified live; `security_auto_away` deliberately not wired), `input_boolean.climate_guest_mode` (created at deploy, id asserted).
- **Memory entries to prune/update at [9]:** `project_stack_b_chain_blueprints_precool_v102_20260908` (NEXT = #19 → done), new chain log for #19, new feedback candidates: (a) HA context inspection is blind for time-pattern automations + coordinator-written integrations (verified live); (b) a pre-existing TCB drift can be *declared* at its sha instead of recomputing the baseline; (c) known-value override detection must quantise to the device step.
- **In-flight sessions touching this surface:** #22 / #24 (observation on the live v1.0.3 — note at deploy), #26 (queued research on epic #25 — untouched), Energy S1 (#29, other repo — shares the TCB drift, resolved there by recompute).
- **Tests required:** in the plan (22 pre-cool + 10 LG new pins; RED 32 / GREEN 312 measured).
- **Convention parity:** legacy `platform:`/`service:` keys kept (both files use them); `{{ }}` booleans; single-lined comparison values; timestamps across boundaries; state-driven notices with `continue_on_error`; per-pillar commits; deploy script untouched — pass.

## 2.5 Live API shape probes (mandatory — done)

`weather.get_forecasts` daily/hourly on `weather.home_sm` (6 / 48 entries, field names and the local-noon UTC stamp); `weather.openweathermap` (no forecast type); entity attributes (`target_temp_step`, `min/max`, `fan_modes`, helper `min/max/step`); HA `Context` on both climate states vs their automations' runs; `last_changed`/`last_updated` after an attribute-only push; `/api/template` renders of every new idiom (`bitwise_and`, `as_local` date match, `states[entity]` item access incl. missing → none, `state_attr('')`, `expand()` last_changed max, list literal, string-form guard, persons loop, indicator `selectattr`); HA `validate_config` on both input-substituted configs (instance values) and on the LG blueprint defaults (empty presence lists); `scripts/deploy-blueprint.sh --dry-run` on both instances. All match the harness and the plan.

## 3. Past failure cross-check

- **Memory entries reviewed:** 296 `feedback_*` descriptions triaged; bodies read where adjacent.
- **Directly relevant (all honoured):** `blueprint_input_rename_breaks_existing_instances` (additive inputs only); `device_state_latch_needs_reset_boundary` (manual-off bounded by the adoption window + restart/vacation guards); `jinja_grid_quantization_raw_float_compares` + "compare against the value the device holds" (P1 above, fixed); `ha_variables_datetime_crosses_step_as_string` (timestamps only); `ha_template_bare_boolean_text_truthy_string` (all `{{ }}`); `ha_choose_default_catchall_new_trigger` (the LG ladder has no `default:`; the new triggers fall through like `update_loop`); `ha_config_flow_helper_entity_id_device_derived` (guest helper id asserted at deploy); `fetch_origin_before_deploy` (T5 step 1); `fixing_a_finding_can_create_the_next_one` (the quantisation fix re-run against the tolerance compare, the string-form guard and the 0.5-grid rows); `gh_repo_view_path_arg_empty_slug` (explicit `-R` everywhere); `codex_chatgpt_account_rejects_gpt54` / `codex_long_prompt_stream_drops_split_payload` / `codex_argv_arg_max_use_stdin` (board dispatch: unpinned, stdin, ≤ ~50 KB parts, idle override); `board_freeze_bundle_all_legs` + `board_bundle_content_assert` (frozen, content-asserted payloads; no edits until synthesis); `agent_dispatch_payload_must_include_body` (R1 reads a frozen file with a sentinel assert — the s13 precedent).
- **Plausibly adjacent:** `ha_bookkeeping_steps_must_not_block_alarm_paths` (the learn write has no `continue_on_error`; it is the last step of its branch and can no longer be rejected — left as is, no unrequested tidying); `harness_worktrees_pollute_repo_globs` (no worktree used).

## 4. Action plan

| Finding | Action |
|---|---|
| P1-1 device-held quantisation | Will fix now — done (434069d) |
| P2-1 cloud-blip manual off | Accepted as-is, documented (spec §3.2 residual 4, requirements, notice text) — operator veto at this gate |
| P2-2 RED count | Will fix now — done |
| P2-3 #22/#24 note | Will fix now — done (T5 step 5, spec §6.5) |
| P3-1..3 | Accepted as-is, recorded |

Operator questions carried forward (no silent decisions): `security_auto_away` not wired (toggle, not state); the deep-night check keeps its correction on a NIGHT_HOLD manual value; "one push max" read as one persistent notification per override; the pre-existing `~/.claude/settings.json` drift declared rather than re-baselined.

## 5. Board (Pass 4.5) — `reviews/board-20260909-113000.md`

Cycle 1: FAIL (3 P1 — R1-01 manual-setpoint latch removing the v1.0.x self-heal; R2-A-04 live presence simulation touching the production EV latch; R2-A-05 rollback captures) + converged P2s → fix wave 79fcf56. Cycle 2 (fresh R1 + R2 on the fix diff): no P0/P1; 1 P2 + 4 P3 from R1, 5 P2 from Codex, two cross-family convergences → all actioned in-session (5a4138a) except two accepted-documented items and one refuted by invariant analysis. **BOARD_VERDICT: PASS · OPEN_P0 0 · OPEN_P1 0 · DEGRADED no.** Terminal state: execute (subagent-driven-development).
