## Board Review 20260909-130652
- mode: design-time
- stakes: standard  |  trigger: default-up — automation logic in bedroom_precool.yaml + tests + instance JSON + docs; no hard trigger (router [0], Entry A, epic #25 / session #26)
- legs: R1=ran (10 findings: 2×P1 7×P2 1×P3; claude-opus-5 via board-reviewer, 837 s, 12 tool uses — verified the plan against bedroom_precool.yaml, the test harness and fan_safety_motion_cutoff.yaml) R2=ran (7 findings: 1×P1 6×P2; codex-cli 0.153.4, account default model gpt-6-astra, reasoning high, 13:07→13:10:21Z; stdin prompt 97 KB; no `-m` pin per Martin 2026-09-05) R3=n/a (design-time) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE=off)
- quorum met: yes
- degraded: no
- base SHA: d1f7c7d
- leg_bundles: {R1:dc1b9ef5f646ca18f30b587337d8f791758192e7bdf939214f4939d8ebbad527, R2:dc1b9ef5f646ca18f30b587337d8f791758192e7bdf939214f4939d8ebbad527}   # canonical (§5.2); re-hash of the dispatched bundle.txt matched at 3a
- main_bundle_sha: dc1b9ef5f646ca18f30b587337d8f791758192e7bdf939214f4939d8ebbad527   # informational (spec + plan + research excerpt, 94 KB, sentinel-asserted, KIND=text)
- finder_rounds: [17]   # standard dial: 1 round (R1 10 + R2 7)
- synthesis_rounds: 1
- cycle: 1 (CYCLE=1, fix_cycles=0)
- panel: {votes: none, framings: none, empirical_checks: 0 (design-time — adjudicated by invariant/interface analysis), inquorate: no} — every finding was ACCEPTED and actioned in the spec/plan; no kill was sought, so no skeptic panel was needed
- tcb_manifest_sha: 294e5c97c5ac2afffe1100b83cc41aa99f3c7b6805edb572a2e3fffaf0a63aea (verify rc=0 at Phase 0c and at [4] entry)
- call_budget: 2/24
- operator_ack: none
- triple-check passes 0–4 (orchestrator, before the board): Pass 0 research-validate present (verdict proceed; all 7 anti-patterns honoured); Pass 1 premise holds (Martin's 7 brainstorm decisions; 5 alternatives in spec §3); Pass 2 found P1 interlock clear-hold + P2 guard read-back (both in d1f7c7d — the read-back was then superseded by the board's settle-window design); Pass 2.5 live probes (LG modes/turn_on restore, fans, cutoff + dimmer instances, forecast); Pass 3 adjacency (kids-repo README line, #19 → v1.2.0, requirements/README/instance); Pass 4 cross-check of 297 feedback memories (latch reset boundary, datetime across a variables step, bare boolean text, input rename, round-trip keys, realized-property tests — all designed around).
- note: R2+R3 are same-family (both Codex) — their convergence counts below cross-family; the Claude-reviews-Claude self-preference residual is open until a true third family (R4) returns. Both converged findings here ARE cross-family (R1+R2).

## VERDICT: PASS (after in-session actions) — open_P0: 0 | open_P1: 0 (3 raised, all actioned in the spec/plan before execution) | P2: 13 (actioned) | P3: 1 (actioned)

Actions landed in commit 0091ab2 (spec + plan). Because the artifacts are design documents, "actioned" means the mechanism was redesigned in the spec and the prescribed YAML/tests in the plan; the code-time board re-reviews the implementation.

## Findings (P0 → P3; converged first)
| id | sev | conf | lens | leg | finding | disposition |
|---|---|---|---|---|---|---|
| R1-01 ≡ R2-03 | P1 | high | correctness / failure path | converged(R1,R2) cross-family | The guard's same-tick read-back (`wait_template` ≤ 10 s, `continue_on_timeout: true`) could time out on a slow cloud restore: corrections skipped on null reads, a null `restored_fan` firing a spurious `set_fan_mode`, the notice claiming success, and — worse — `ac_is_running` then latching a stale DRIVE park (18 °C / high) all night with no retry path. | ACTION: read-back removed. New **guard settle window**: for 15 min after ANY night start (`guard_settle_due`), each tick asserts the parked state from the existing STEP 2a live reads — mode (`settle_mode_due`), setpoint only when outside ± correction_step (never fights the deep-night nudge), fan when known and different. No wait, no timeout, no null read; a late restore is corrected on a later tick. Tests pin all three predicates incl. the nudge and the closed window. |
| R1-03 ≡ R2-04 | P1 | high | logic / invariant | converged(R1,R2) cross-family | On a cool-day night the lock never parks the unit, so a guard `turn_on` restores whatever mode was last used — `heat`, `fan_only`, `auto` — and the running-unit latch would hold a heater in a 24.6 °C child's room until wake. The spec's "manual choice earlier that day" premise was false. | ACTION: `settle_mode_due` sets `desired_mode` whenever the running unit is not in `cool`/`dry`; spec §2.5 rewritten; test pins `heat` and `fan_only` restores. |
| R2-01 | P1 | high | safety / TOCTOU | R2 | The due lists are computed in STEP 2c; STEP 6 (climate calls) and the guard's wait sat between the snapshot and the fan command, so an adult entering or the cutoff switching the fan off in that gap could not cancel an already-approved `turn_on`. | ACTION: the fan step moves to STEP 5c, BEFORE the AC dispatch (nothing between snapshot and command), and every `repeat` iteration re-checks the whole rule on live state (availability, target, interlock incl. clear hold, untouched-since-reference) immediately before the service call. Test pins the position and the live re-check template per call. |
| R2-02 | P2 | high | latch | R2 | A cloud read stuck on `off` while the unit runs makes `guard_due` true every minute → a `turn_on` beep per tick; the "one no-op beep" claim was wrong. | ACTION: `guard_due` requires a 5-minute tick (`now().minute % 5 == 0`): ≤ 12 attempts/h in the pathological case, each notified; ≤ 4 min delay in the normal case (≈ 0.02 °C). Documented in spec §2.2/§2.5. |
| R1-02 | P2 | high | conventions | R1 | `restored_fan != night_fan_mode` with a null read-back fires a spurious fan command. | ACTION: superseded — the settle step uses `current_fan` (already normalised to `'unknown'`) and requires `current_fan != 'unknown'`; test pins the null case. |
| R1-04 | P2 | high | correctness | R1 | `s.attributes.percentage` on a fan without the attribute is a Jinja `Undefined`; `int(-1)` does not catch `UndefinedError`, so the whole STEP 2c variables step (and all AC control) would die. | ACTION: `state_attr(f, 'percentage') \| int(-1)` (repo idiom) in all three due templates; text pin forbids `s.attributes.percentage`; a no-percentage fan fixture in the tests. |
| R1-05 | P2 | high | correctness | R1 | The pre-existing validation test renders the STEP 3 template without `fan_settle_minutes` → Undefined → raises; T1's PASS gate did not account for it. | ACTION: T1 edits that test's `base` (`fan_settle_minutes=45`) and states the count. |
| R1-06 | P2 | high | logic | R1 | `fans_unset_night` ⊇ `fans_due_night`, so on the last settle tick a fan that is being commanded is also reported as "not applied, nothing will retry". | ACTION: `fans_unset_night` = untouched ∧ not at target ∧ (blocked ∨ unavailable) — disjoint from due by construction; disjointness test over four fixtures. |
| R1-07 | P2 | high | logic | R1 | The fan-only lock switched the unit off even when the warmest room was already over the band, so the guard re-fired next tick: off + on for nothing. | ACTION: `turn_off` gated on `not (warmest > ideal + tolerance)`; on such a night the unit stays on (ac_hold behaviour); rendered test. |
| R1-08 | P2 | medium | correctness | R1 | `delta_in` and `cooling_needed` stayed keyed on `ideal_temp`: the lead under-predicts by `prechill_offset × k_indoor` (7.5 min) on fan-only nights and the shared bias absorbs it; a room between the two targets never pre-chills. | ACTION: both key on `bedtime_target` (moved to STEP 2a so it precedes them); rendered test for the lead term, the skip gate and the substate; `ac_hold` unchanged (target = ideal). |
| R1-09 | P2 | medium | correctness | R1 | Clear hold 3 min = the cutoff's resume instant → racing writes (cutoff 21 %, ours 1 %) within Tuya lag; nothing enforces hold ≥ cutoff hold across the two repos. | ACTION: default and instance `interlock_clear_minutes` 5 (cutoff 3 + 2); documented deploy-time invariant, checked in the deploy live-verify (read the cutoff instance's `hold_duration`). |
| R2-05 | P2 | high | testing | R2 | `_S` tied `last_changed = last_updated`, so tests could not tell a fan-touch keyed on the wrong timestamp from an interlock hold keyed on the wrong one. | ACTION: independent timestamps in `_S`; tests split them (percentage change with old `last_changed` = touched; sensor attribute update inside the hold with old `last_changed` = not blocked). |
| R2-06 | P2 | high | parity | R2 | A lead window that starts before noon (long lead cap) flips the noon-pivot parity mid-PRECOOL. | ACTION: parity pivots at WAKE TIME (today from wake, yesterday before it); noon validation dropped; test covers 11:00 → 01:00 → 07:14 same parity, 07:15 flips. |
| R2-07 | P2 | high | containment | R2 | A failing `fan.turn_on` (cloud error) aborts the sequential repeats and the rest of the tick. | ACTION: `continue_on_error: true` on every fan service; test pins it. |
| R1-10 | P3 | high | consistency | R1 | Beep-budget numbers disagreed between spec and plan; the 01:00-window worst case ignored the corrections. | ACTION: reconciled: fan_only lock ≤ 4 (typically 1), guard nights turn_on 1 + settle ≤ 3 + nudge ≤ 1 (typical 1–2, worst 5); same numbers in requirements/README edits. |

Nits: 0 (cap 5). Dismissal memory: none matched. Single-leg findings retained at their raised severity (default keep; all accepted by the operator side, no refutation attempted).
