# Board Review — 20260909-113000
mode: design-time | stakes: standard (trigger: default-up — automation logic in two blueprints + tests + instance JSON + docs; no hard trigger; deploy script pinned via TCB_EXTRA and untouched) | entry: C | session: leviemartin/Blueprints_Home #19 (epic #18)
legs: R1=ran (Claude Opus 5, board-reviewer, 8 findings) · R2=ran ×3 parts (Codex, account default resolved to **gpt-6-astra** on codex-cli 0.153.4; 9 + 5 + 3 findings; 24–52 s per part) · R3=absent by design (design-time) · R4=reserved · PonyTail=skipped (the plan is already the smallest correct diff; standard dial)
quorum met: yes | degraded: no
base SHA: 434069d (cycle 1) → 79fcf56 (cycle-2 delta payload) → 5a4138a (post cycle-2 fixes, the state that ships to execution)
leg_bundles: {R1: 2361b9aa8ad1b4a8bcbcaf3b85c7d794ba404f9e4959f720a820271bed2654a6 (bundle-full.txt, frozen file read by the leg, sentinel asserted), R2-A: c753093f13510caea9768da883ae42ed065ba8802bca81450a6c945d3cc05d04, R2-B1: e2fabc8b7b764dc1a7847fb9e44b6bcd458ebf558d50fa1e0cf1d030a25d32c3, R2-B2: a1d3e744a06fe526b09b955be288b106096172cc883af2d53d18bc8d34598acd, R1-delta: c2347af161a178ff624bf7709f8496995b0f9556e72246186a1b47cd58920b69, R2-delta: ec53be9e420486eeb9b1cfa1505b9514c91242afbcac7b82965b014b023c052e}
main_bundle_sha: 2361b9aa8ad1b4a8bcbcaf3b85c7d794ba404f9e4959f720a820271bed2654a6 (informational)
finder_rounds: [25] (standard: 1 round) | synthesis_rounds: 1 | cycle: 2 (fix_cycles = 1; parent_bundle_sha = main_bundle_sha, delta_bundle_sha = R1-delta / R2-delta above) | panel: none convened (no contested P0/P1 — every P1 was accepted and fixed; no empirical checks needed at design-time) | call_budget: 6/24 | operator_ack: none
tcb_manifest_sha: 94cccc64bc85efa5b38309bca448ff5dd1591f7c7598391ebd0a6e8a9b66caa7 — verify rc 0 with the declared entry `effe49ce87beb46086fc2973efe2c145d2a48b447c62dfbd38beb51e0c571436  /home/martin/.claude/settings.json` (pre-existing drift, declared not recomputed; see the plan header)
payload split note: R2 reviewed three ≤ 63 KB parts (A = spec + plan prose; B1 = pre-cool YAML + JSON + docs diffs; B2 = LG YAML + both test diffs), each with its own sentinel and preflight; R1 reviewed the full 168 KB bundle. Codex model unpinned per operator policy (2026-09-05); the account default was gpt-6-astra for all four Codex dispatches.
note: R2+R3 same-family convergence rule not exercised (no R3 at design-time); the Claude-reviews-Claude residual is open until a true third family (R4) returns.

## VERDICT (cycle 1): FAIL — open_P1: 3 (R1-01, R2-A-04, R2-A-05) + converged P2s → fix wave 79fcf56 → cycle 2 delta re-review (fresh R1 + R2) — result recorded below.

## Cycle-1 findings (25 raw → 19 distinct after convergence), with dispositions

| # | ids | sev | conf | lens | finding | disposition |
|---|---|---|---|---|---|---|
| 1 | R1-01 + R2-A-02 | **P1** | high (cross-family) | correctness | `manual_setpoint` latched on ONE tick of a lagged/lost setpoint command at turn-on and the PRECOOL gate suppressed the retry that v1.0.3 relied on → a whole-evening stand-down | **FIXED**: `ac_state_age_sec >= 120` two-tick grace; test rows 60/119/120 s; requirements + spec residual rewritten |
| 2 | R2-A-04 | **P1** | high | rollout | the live LG proof injected `person.*` states and switched the EV latch off — the EV-charger guard (`sec_ev_cutoff`/`sec_ev_absence`) is live and would cut the plug | **FIXED**: isolated `device_tracker.climate_test_*` entities on a temporary instance variant; no production security entity written |
| 3 | R2-A-05 | **P1** | high | rollback | rolling `deploy/<id>.prev.json` backups are overwritten by the temporary deploy; `main~1` is not the pre-change revision | **FIXED**: immutable copies of both live instance configs + both shipped YAMLs (`chmod -w`), rollback pinned to `5c5a08b` with save/POST/verify steps |
| 4 | R1-04 + R2-A-01 + R2-B1-002 | P2 | high (cross-family) | boundary | helper range above 120 (floor > ceiling) or fractional bounds still produced a rejected write | **FIXED**: ceil/floor whole-number bounds inside the intersection, round-before-clamp, write gated on `bias_range_valid`, notice says when nothing is written |
| 5 | R1-02 + R2-B1-004 | P2 | high (cross-family) | boundary | 60 s HA-start grace too short for a cloud integration's first state; `this` untested at runtime; a reload after a manual off ends the hold | **FIXED**: 600 s; `this.last_changed` proven live (throwaway automation); reload-ends-hold documented |
| 6 | R1-08 + R2-B2-02 | P2 | high (cross-family) | logic | the away delay measures the CURRENT state, so a zone→zone move re-arms it | **ACCEPTED-DOCUMENTED**: no stateless "last time home" exists; bounded to one delay of comfort; input description, comments, requirements and a test row state it |
| 7 | R2-A-03 + R2-B2-01 | P2 | high (same-family) | trust | home indicators unknown/unavailable/missing read as off → setback enabled | **FIXED**: any indicator not exactly `off` holds comfort; string-form guard; tests |
| 8 | R2-B1-001 + R2-A-09 | P2 (R2 said P1) | medium | boundary | malformed forecast entries raise inside templates (the fetch's `continue_on_error` does not cover them) | **FIXED** in both loops (`as_datetime(ts, none)`, `float(none)`); severity adjudicated P2 — a malformed successful response from the core Met.no integration is hypothetical and the hourly loop had the same exposure since v1.0.0 |
| 9 | R2-B1-005 | P2 | high | quantisation | idempotency guards compared the unquantised desired setpoint | **FIXED**: `effective_drive`, `maintaining_setpoint`, `deep_target_setpoint` quantised at the source (int on a whole-degree step) |
| 10 | R2-B1-003 | P2 | high | attribution | vacation guard's 5 s window vs coordinator lag | **FIXED**: 120 s |
| 11 | R1-03 | P2 | high | consistency | plan pinned `31 inputs` (transcribed, not observed) | **FIXED**: observed `30 inputs`; scratch validation re-run, hashes re-pinned |
| 12 | R2-A-07 | P2 | high | validation | criterion 2 relied on `last_changed`, which a same-value write does not move | **FIXED**: the 19:29 trace's executed `input_number.set_value` step is the evidence |
| 13 | R2-A-08 | P2 | high | validation | manual triggering does not prove the return triggers or the debounce | **FIXED** (partial): natural `indicator_on`/`presence_return` runs + the real 10-min debounce on isolated trackers; climate commands not exercisable in this weather (recorded) |
| 14 | R2-B2-03 | P2 | high | testing | band injected directly into the target_mode test | **FIXED**: chained presence → band → target_mode test |
| 15 | R2-A-06 | P2 | high | tcb | the settings.json drift declaration lacks a full semantic diff | **OPERATOR** (accepted-documented): the e12b55db content is not retained anywhere; verified facts stated; Martin's acceptance at this gate |
| 16 | R1-05 | P3 | medium | boundary | DAY_OFF turn_off on the last pre-window tick confirmed after the window opens | **FIXED**: 120 s margin on the window start |
| 17 | R1-06 | P3 | high | logic | STEP 2 validation gates read the widened band | **FIXED**: gates + messages on `temp_low_cfg`/`temp_high_cfg` |
| 18 | R1-07 | P3 | high | conventions | requirements numbering 1,2,4,3 | **FIXED** |
| 19 | — | — | — | — | (R2-A-02 merged into #1) | — |

Nit cap: 0 nits reported. Dismissal memory: no `feedback_board_dismissed_*` entry matched. Auto-refutation: none (design-time).

## Cycle-2 delta re-review (fresh legs; payloads preflighted KIND=text, sentinels DELTA / DELTA2; R1 = Claude Opus 5 on `delta-full.txt` 88,396 B, R2 = Codex gpt-6-astra on `delta-r2.txt` 88,060 B, 42 s)

Legs: R1D=ran (5 findings: 1 P2, 4 P3) · R2-C2=ran (5 findings: 5 P2). No P0/P1. Cross-family convergence on two items.

| # | ids | sev | disposition |
|---|---|---|---|
| D1 | R1D-01 | P2 | **FIXED**: legacy `device_tracker.see` trackers persist in `known_devices.yaml` (not editable on the HA box) — Task 5 now uses two throwaway **persons** (WS `person/create`, REST-injected state, WS `person/delete`); the whole path was probed live 2026-09-09 12:13Z (create → `unknown`, inject `not_home` → persists + renders with its own `last_changed`, delete → "Entity not found"). Side effect while they exist (resolver roster count 3 → fails only toward not-arming; auto-Away is off) recorded in the plan. |
| D2 | R1D-02 + R2-C2-01 | P3/P2 (cross-family) | **FIXED**: the daily "first usable" entry now also needs a parsable datetime (`fdt` computed before either assignment); test rows flipped (`bad[:3]` → None; junk-first + valid-later → 18.6). NaN/±inf and tz-naive stamps: not reachable from the core Met.no integration (typed floats, tz-aware ISO) — accepted-documented, not coded. |
| D3 | R1D-05 + R2-C2-02 | P3/P2 (cross-family) | **FIXED**: an indicator collection that is not a list (string form / non-iterable) now holds comfort (`{{ true }}`); a genuinely empty list still permits the setback; test asserts `away_active` False for the string form. |
| D4 | R1D-03 | P3 | **FIXED** (wording): comment, input description, spec and requirements now say EVERY presence state change restarts the delay (a multi-zone commute postpones the setback until one delay after the last change); two extra test rows. |
| D5 | R1D-04 | P3 | **FIXED** (wording): spec §3.2 residual (2) rewritten to the post-grace statement (two consecutive lost commands / an acknowledgement delayed past 120 s). |
| D6 | R2-C2-03 | P2 | **ACCEPTED-DOCUMENTED**: the two-tick grace is an explicit 120 s acknowledgement timeout; an acknowledgement delayed beyond it reads as manual for that night (requirements + spec residual (2) say so). No stateless pending-command ledger exists; design B (helper) shares the class. |
| D7 | R2-C2-04 | P2 | **REFUTED by invariant analysis** (design-time, no vote needed — the leg's own premise is contradicted by the artifact): `vacation_changed_ts` is the toggle's LATEST change, so a vacation that ends after the unit went off (the blueprint's own turn_off hours earlier) always reads as not-manual regardless of confirmation lag; only a toggle-on/off pair inside 120 s could misattribute. Pinned by two new test rows (off at 12:00/16:00, vacation toggled at 16:30 → not manual). |
| D8 | R2-C2-05 | P2 | **FIXED** (test): the chain now continues into the ladder — the acting branch is selected by rendering its own condition and its `desired_setpoint` is rendered from the presence-derived band (away: off / heat 20.5 / cool 24.0; home: heat 22.5). Live climate commands remain weather-bound (recorded). |

Post-fix state (79fcf56 → this commit): suite 316 passed; per-file 44 / 59; RED at the T1 state 37 failed / 66 passed; dry-runs 9 / 30 inputs; HA `validate_config` valid ×3; live renders of the changed idioms match; plan extraction reproduces all nine sha256 pins.

## VERDICT (cycle 2): **PASS** — open_P0: 0 | open_P1: 0 | P2: 0 open (8 fixed, 3 accepted-documented, 1 refuted) | P3: 0 open
- cycle-1 P1s (R1-01, R2-A-04, R2-A-05) all fixed and re-reviewed by both families; no new P0/P1 raised on the fix wave; the cycle-2 P2/P3 items were actioned in-session (fix diff stays inside the reviewed manifest — no third convening needed, §4.5).
- Operator items carried to the session report (not silent): `security_auto_away` not wired; the deep-night check keeps its correction on a NIGHT_HOLD manual value; "one push max" read as one persistent notification; the pre-existing `~/.claude/settings.json` drift declared at its sha (R2-A-06: a full semantic diff is impossible — content not retained).
- Codex model per leg: gpt-6-astra (all four dispatches; account default, unpinned per operator policy 2026-09-05).
- call_budget: 6/24 · finder_rounds: [25] · synthesis_rounds: 1 · CYCLE=2 (fix_cycles=1) · panels: none · empirical checks (orchestrator-run, admissible at design-time as fact probes): `this.last_changed` in action variables; throwaway-person path; HA renders of `round(0,'ceil')|int`, `as_datetime(x, none)`, `float(none)`, state-age, indicator loop, string-form guard.
