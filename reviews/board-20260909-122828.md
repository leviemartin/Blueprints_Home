# Board Review — 20260909-122828
mode: code-time | stakes: standard (trigger: default-up — automation logic in two blueprints + tests + instance JSON + docs; no hard trigger; deploy script pinned via TCB_EXTRA and untouched) | entry: C | session: leviemartin/Blueprints_Home #19 (epic #18) | design-time board: 20260909-113000 (PASS, cycle 2)
base SHA: f205cf0 (pre-code) → head 611d7c4 (code commits eba0e51, ef50405, d253748, 2c12291, dd37901, beae5c9) → post-board fix wave a1930f6
legs: R1=ran (Claude Opus 5, board-reviewer, frozen `code-full.txt`, 5 findings: 1 P2 + 4 P3) · R2=ran ×3 parts (Codex, account default **gpt-6-astra**, codex-cli 0.153.4; P1 = 1 finding, P2 = 0, P3 = 2; 9–22 s per part) · R3=absent (standard dial — code-time R3 runs at high-risk only) · R4=reserved · PonyTail=skipped (`PONYTAIL_DEFAULT_MODE` unset → off)
quorum met: yes | degraded: no
leg_bundles: {R1: 14790ad497f05c4bd66b5398b2b395b50e676922606bfe4205d4853f32fa8e33 (code-full.txt), R2-P1: f904290da3ecbf0939b97adfd50a673cfa230d90cdf44939c12c53d99f12d959 (pre-cool YAML + instance), R2-P2: c4da5739fca55a10fce8531c4f91f85c6cd28742e8bf866b2b86cc6141f82e4e (LG YAML + instance + docs), R2-P3: 0e9bef622d55f09403158b3eb7891a0b3cfc07722dc5109b1cb3f698a6e6a331 (both test files)} — every dispatched payload re-hashed at synthesis and equal to its authorized sha (frozen files; R1 read the file and asserted the sentinel)
main_bundle_sha: 14790ad497f05c4bd66b5398b2b395b50e676922606bfe4205d4853f32fa8e33 (informational)
finder_rounds: [8] (standard: 1 round) | synthesis_rounds: 1 | CYCLE=1 (fix_cycles=0) | panel: none (no contested P0/P1 — none raised) | call_budget: 4/24 | operator_ack: none
tcb_manifest_sha: 94cccc64bc85efa5b38309bca448ff5dd1591f7c7598391ebd0a6e8a9b66caa7 — verify rc 0 at [6] with the declared entry `effe49ce…  /home/martin/.claude/settings.json`
preflight: all four payloads KIND=diff, sentinel-asserted, content-asserted (`OK bundle=` lines in `reviews/.board-20260909-122828/preflight.log`); probe age 900 s at dispatch
pre-board orchestrator checks (review-shipped §2–§4.5): fresh suite 317 passed; dry-runs 9 / 30 inputs; removed-code audit (only the dead `forecast` fallback, the re-indented PRECOOL block, the old validation strings, the two R1-04 hand-rolled tests); no dependency or secret exposure; mutation spot check (removing the two-tick grace fails `test_rendered_manual_setpoint_is_any_value_the_blueprint_could_not_have_commanded`); pillars present: code, tests, instance JSON, docs, spec, plan (verification-before-completion walked per pillar — see the SDD ledger); not touched: audit/backlog/horizon/convention. The SDD chain before the board: batched Sonnet implementer (hash-pinned), Sonnet task review (clean), Opus whole-branch review (2 Important → 1 ruled operator decision, 1 plan fix; 7 Minor → fix wave dd37901/beae5c9, scoped re-review clean).
note: R2+R3 same-family rule not exercised (no R3); the Claude-reviews-Claude residual is open until a true third family (R4) returns.

## VERDICT: PASS — open_P0: 0 | open_P1: 0 | P2: 3 (should-fix, actioned post-board in the same session) | P3: 5 (4 actioned, 1 accepted-documented)

## Findings (P0 → P3; converged first)

| # | ids | sev | conf | lens | finding | disposition |
|---|---|---|---|---|---|---|
| 1 | R2-01 | P2 | high | boundary | `forecast_daily_list` (and the pre-existing `forecast_list_safe`) accept `forecast: null` / a scalar / a string / a mapping (`is defined` only) → the iterating template raises and aborts the run before the climate commands; the fetch's `continue_on_error` does not cover it | **FIXED post-board**: both extractions also require `is iterable and not string and not mapping` (and the entity value to be a mapping); rendered rows for every malformed shape |
| 2 | R1-01 | P2 | high | correctness | a presence / home-indicator entity that does not exist holds comfort silently (`states[e] is none` → hold) — the instance wires `input_boolean.climate_guest_mode`, which exists only after the deploy step creates it, so a skipped step ships the feature inert with no signal | **FIXED post-board**: STEP 1 `presence_missing` + STEP 2d self-healing notice `ac_climate_presence_entity_missing` naming the entities (fail-toward-comfort kept); tests for the list and the block shape; the deploy task already creates and asserts the helper first |
| 3 | R2-P3-01 | P2 | high | testing | no test drives the response → list extraction (`forecast_*_list`); a lookup that always yields `[]` would pass | **FIXED post-board**: `test_rendered_forecast_lists_are_extracted_from_the_response_or_empty` (absent / other entity / null / scalar / string / mapping / non-mapping entity / real list) |
| 4 | R1-05 | P3 | high | coverage | the known-set test hand-fed `maintaining_setpoint=21.5` with a whole-degree step — a context the blueprint cannot produce after source quantisation; the two quantisation tests jointly asserted an inconsistent known-set / deep-target pair | **FIXED post-board**: the test renders `effective_drive` / `maintaining_setpoint` / `known_setpoints` from the blueprint and asserts every rendered `deep_target_setpoint` is a member of the rendered known set (steps 1.0 and 0.5) |
| 5 | R1-03 (+ final review #3) | P3 | high | boundary | `earliest_turn_on_ts` re-anchors at local midnight, so `manual_off` reads false from 00:00 and the STEP 7e notice self-dismisses while the unit is still held off | **FIXED post-board**: before wake time the instant refers to yesterday's window (the `bedtime_ts` pattern mirrored); post-midnight test rows |
| 6 | R1-04 | P3 | medium | correctness | `this is defined` is true when HA supplies `this = None`; `this.last_changed` is then Undefined and `as_timestamp()` without a default raises, killing the STEP 2c block for the tick | **FIXED post-board**: `(this.last_changed \| as_timestamp(0)) if (this is defined and this is not none) else 0`; test rows for none / undefined / a state |
| 7 | R2-P3-02 | P3 | high | boundary | the local-date test row has UTC date = local date, so an implementation without `as_local` passes | **FIXED post-board**: a 22:30Z-on-the-9th entry (00:30 local on the 10th) selected as "today" at 00:30 local on the 10th |
| 8 | R1-02 | P3 | high | logic | the deep-pull infeasibility notice reads the effective band; a marginal config could report infeasible only while someone is home (widening can only relax the gates) | **ACCEPTED-DOCUMENTED**: comment + requirements state the presence dependency; cfg-band twins would add four variables for a diagnostic (the live config is feasible on both bands) |
| — | scoped re-review minor | P3 | — | wording | STEP 7a notice rendered "the configured one () does not exist" when no helper is configured | **FIXED post-board**: the sentence branches on `lead_bias_configured` |

Nit cap: 0 nits. Dismissal memory: no `feedback_board_dismissed_*` entry matched. Auto-refutation: none needed (no finding contradicted a passing test). Noise control: every finding quoted the artifact verbatim; none excluded.

## Post-board actions (same session; verdict PASS → should-fix P2s + P3s actioned; no delta cycle required — no P0/P1, the fix diff stays inside the reviewed manifest)
- Fix wave commits: `9a9a1b8` (pre-cool YAML + tests), `a2aa32e` (LG YAML + tests), `a1930f6` (requirements); suite 321 passed; both dry-runs green; HA `validate_config` valid ×3; live renders of the new idioms match; scoped re-review (Sonnet) on 611d7c4..a1930f6: all nine items ADDRESSED, no new breakage (index-pinned LG tests re-parsed and confirmed; the old `this`-none template reproduced the UndefinedError; the post-fix templates re-simulated); out-of-scope note: one long comment line in lg_ac_climate.yaml (cosmetic).
- Deferred to the operator report: EV latch as a home indicator (final review #1 — locked #19 wiring); `security_auto_away` not wired; the deep-night check's literal boundary reading; "one push max" = one persistent notification; the settings.json drift declared at its sha.
- Rollback record: the pre-deploy immutable copies are taken at plan Task 5 Step 2 (`~/AI/reviews/deploy-19-live-before-<ts>/`); rollback revision `5c5a08b`.

## Close
- BOARD_VERDICT: PASS · OPEN_P0: 0 · OPEN_P1: 0 · DEGRADED: no · CYCLE 1 (no delta cycle needed) · Codex model per leg: gpt-6-astra ×3 · call_budget 4/24.
- code-review-gate: no debug artifacts / TODOs / commented-out code in the code range; no unnecessary tracked files; conventions per the task + whole-branch reviews; origin/main unchanged since chain start (no merge needed).
- Terminal: [7] PR `Session: #19` → merge → [8] plan Task 5 (deploy + live-verify) → observe → [9] closing-session.
