## Board Review 20260908-090730
- mode: design-time
- stakes: standard  |  trigger: default-up — automation logic change in bedroom_precool.yaml + test + instance JSON + docs; no hard trigger (router [0], Entry B)
- legs: R1=ran (8 findings; claude-opus-5 via board-reviewer, 352 s) R2=ran (2 findings; codex-cli 0.153.4, account default model gpt-6-astra, reasoning high, 34 s — no `-m` pin per Martin's 2026-09-05 policy) R3=n/a (design-time) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE=off)
- quorum met: yes
- degraded: no
- base SHA: 4bd8041
- leg_bundles: {R1:9a71a8a22aaf0f1bc26eb5605d097a0f358a170da7b890a267e3ba04c24668d0, R2:9a71a8a22aaf0f1bc26eb5605d097a0f358a170da7b890a267e3ba04c24668d0}   # canonical; re-hash of the dispatched bundle.txt matched at 3a
- main_bundle_sha: 9a71a8a22aaf0f1bc26eb5605d097a0f358a170da7b890a267e3ba04c24668d0
- finder_rounds: [10]   # standard dial: 1 round (R1 8 + R2 2)
- synthesis_rounds: 1
- cycle: 1 (CYCLE=1, fix_cycles=0)
- panel: {votes: none, framings: none, empirical_checks: 0 (design-time — adjudicated by invariant/interface analysis), inquorate: no} — the one P1 is converged cross-family and accepted; no adjudication needed
- tcb_manifest_sha: dc110c47de26fc65a046de31d918f4202236a801b20120fdf01abd22dee962cd (verify rc=0 at Phase 0c)
- call_budget: 2/24
- operator_ack: none
- triple-check passes 0–4 (orchestrator): Pass 0 research-validate DEFERRED by the operator (recorded in the spec header); Pass 1 premise holds (Martin-decided input; 3 alternatives listed in spec §3); Pass 2 robustness — additive input, resolver fallback, no booleans, no datetime across the boundary; Pass 2.5 live probe — `climate.bedrooms` fan_modes ["auto","low","medium","high"]; Pass 3 adjacency — requirements table, README beep bullet, instance JSON, #19 decision comment, lg_ac_climate untouched; Pass 4 cross-check — bare-boolean text, blueprint-input-rename (additive only), folded-scalar whitespace (single-lined), realized-property tests, HA variables literal_eval.
- note: R2+R3 are same-family (both Codex) — their convergence counts below cross-family; the Claude-reviews-Claude self-preference residual is open until a true third family (R4) returns. Both converged findings here ARE cross-family (R1+R2).

## VERDICT: PASS (after in-session actions) — open_P0: 0 | open_P1: 0 (1 raised, actioned in the spec/plan before execution) | P2: 2 (actioned) | P3: 5 (actioned)

## Findings (P0 → P3; converged first)
| id | sev | conf | lens | leg | finding | disposition |
|---|---|---|---|---|---|---|
| R1-01 ≡ R2-01 | P1 | high | completeness / interface | converged(R1,R2) cross-family | The spec/plan omit `night_fan: !input night_fan` in the top-level `variables:` pass-through; without it the Jinja variable is Undefined and `night_fan_mode` silently renders `fan_normal` (medium) — the feature is a no-op and the planned render test would stay GREEN. | ACTION: spec §2 + plan Step 2 add the binding; plan Step 1 adds a pass-through pin (every declared input appears in top-level `variables:` as `!input <name>`). |
| R1-02 ≡ R2-02 | P2 | high | correctness / invariant | converged(R1,R2) cross-family | "Beep budget unchanged" is false on the nominal path: a HOLD-ending night issued zero fan commands at the lock in v1.0.2 and issues one with night_fan=low. Requirements BEDTIME-LOCK row (0–1) and README "0–2 after bedtime" already under-count the branch (mode + setpoint + fan). | ACTION: spec bullet restated; requirements row → "≤ 3 (mode, setpoint, night fan; typically 1–2)", after-bedtime total → "lock ≤ 3 + deep-night ≤ 1"; README bullet reconciled. Accepted increase: +1 command on HOLD-ending nights (Martin's choice of low). |
| R1-03 | P2 | high | conventions | R1 | Exact-case `night_fan in ac_fan_modes` contradicts the repo's case-tolerant fan-mode convention (fan_high/fan_normal candidate lists; lg_ac_climate.yaml); a unit reporting 'Low' resolves to medium silently. | ACTION: `night_fan_resolved` loops `ac_fan_modes` comparing `lower`; `night_fan_mode` = resolved or fan_normal; LG-specific options pin dropped for realized-output assertions incl. 'Low'/'Mid' units and a string-form `ac_fan_modes` (no substring match). |
| R1-04 | P3 | medium | conventions | R1 | Fallback to fan_normal is invisible. | ACTION: STEP 7c notice `bedroom_precool_night_fan_unsupported` (enable_notifications + enable_fan_control + modes discovered + unresolved). |
| R1-05 | P3 | high | conventions | R1 | Gated input must name its gate. | ACTION: night_fan description "Only used when Enable Fan Control is on"; enable_fan_control description mentions the night fan. |
| R1-06 | P3 | high | correctness | R1 | Variable order load-bearing, unpinned. | ACTION: order pin fan_normal → night_fan_resolved → night_fan_mode. |
| R1-07 | P3 | medium | testability | R1 | Harness cannot express the branch-level pin. | ACTION: recursive branch walker in the test (resolve by phase condition, collect `climate.set_fan_mode` steps, assert data per branch). |
| R1-08 | P3 | low | logic | R1 | DRIVE-at-bedtime: the lock cuts high → low on a hot night; the deep check never touches the fan. | ACTION: spec §1 records the accepted trade-off (Martin's default; per-instance `night_fan: medium`/`high` is the lever). |

Nits: 5 (cap 5). Dismissal memory: none matched.
