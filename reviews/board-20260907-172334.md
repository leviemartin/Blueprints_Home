# Board Review — 20260907-172334
- mode: code-time
- stakes: standard | trigger: default-up — blueprint YAML + tests + instance JSON + docs; no hard trigger matched (deploy script pinned, not edited; no deletion; no deps)
- artifact: branch `bathroom-heating-rack-v2` 0b7d02f..af315de (session #13, epic #10, Entry B) — bathroom_heating_rack.yaml v2.0.0, tests/test_bathroom_heating_rack_structure.py, deploy/bathroom_heating_rack_1776551429917.json, requirements_bathroom_heating_rack.md, README.md, spec §4.1 delta
- legs: R1=ran (4 findings, Claude Opus 5 `board-reviewer`, dispatched against the frozen payload file with a mandatory presence check; 199k tokens) R2=ran (4 findings, Codex **gpt-6-astra** — no model pin per operator policy 2026-09-05; codex-cli 0.153.4) R3=n/a (standard dial — auto only at high-risk) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE unset → off)
- quorum met: yes
- degraded: no
- base SHA: af315de (branch head; merge-base with main 0b7d02f)
- leg_bundles: {R1: 3e8821002346302adbf2690828520a489b0b30af718a0d5ee518b1619f7a15e4, R2: 0406d6085eaf72eca38be53ba82d3ba74b823806c6620dce7901f297f9984205}   # canonical (§5.2); R2 = same diff minus the pytest file
- main_bundle_sha: 3e8821002346302adbf2690828520a489b0b30af718a0d5ee518b1619f7a15e4 (informational)
- finder_rounds: [8]
- synthesis_rounds: 1
- cycle: 1 (fix_cycles = 0)
- panel: {votes: none — no contested P0/P1 (all P2/P3), framings: none, empirical_checks: 2 (HA validate_config on the fixed YAML; live /api/template render of the missing-entity guard from the SDD fix wave), inquorate: no}
- tcb_manifest_sha: 94cccc64bc85efa5b38309bca448ff5dd1591f7c7598391ebd0a6e8a9b66caa7 (verify rc=0 at Phase 0c)
- call_budget: 2/24 (R1, R2)
- operator_ack: none
- probe: 2026-09-07T17:24:33Z (claude + codex reachable); both preflights OK, KIND=diff, SENTINEL="not boost_active and"
- prior gates: design board 20260907-161235 PASS (13 findings actioned/adjudicated); SDD final review (Opus) 0 Critical / 0 Important / 8 Minor → fix wave 743254c re-reviewed clean
- verification gate (4.5): pillars code, tests, docs, spec, plan(audit), reviews — suite 256 passed (fresh shell), deploy dry-run ok (18 inputs), HA validate_config valid on the input-substituted config, no secret-shaped strings / TODO residue in code files; backlog/horizon/convention pillars not touched
- note: R2+R3 are same-family (both Codex) — their convergence counts below cross-family; the Claude-reviews-Claude self-preference residual is open until a true third family (R4) returns. R3 does not run at standard dial.
- integrity: dispatched payloads re-hashed before synthesis — R1 `3e882100…` match, R2 `0406d608…` match.
- fix wave: `8125c6d` (YAML/tests/docs, Sonnet fixer) + spec `3e2fa31` on `bathroom-heating-rack-v2`; rack `PASS=127 FAIL=0`, suite `PASS=260 FAIL=0`, deploy dry-run ok, HA `validate_config` valid on the input-substituted fixed config; scoped re-review: see below.

## Verdict

**BOARD_VERDICT: PASS** — OPEN_P0: 0 · OPEN_P1: 0 · DEGRADED: no.

No P0/P1 was raised by either leg. The four P2s were actioned post-board in the same session (fix wave `8125c6d`, spec `3e2fa31`) except one (R1-01) accepted-documented and down-ranked to P3 with a recorded rationale; the fix diff stays inside the reviewed manifest, so no delta board cycle is required (precedent: board 20260907-163522). A scoped re-review (Sonnet) verifies the fix wave; its verdict is recorded at the end of this report.

## Findings (sorted P2 → P3; cross-family convergence first)

| id | sev | conf | lens | source | path / section | finding (evidence) | refutation_status | disposition |
|---|---|---|---|---|---|---|---|---|
| B20260907-172334-01 | P2 | high | logic/boundary | converged(R1-01, R2-002) — cross-family | STEP 2 `<p>_heating` | slot "heating" was inferred from setpoint VALUE alone: a setpoint retained through a vacation (device off) latches the next occurrence (R2-002); two same-phase slots sharing a target on the same day share the latch (R1-01) | none | **FIXED** for the vacation/off case — `<p>_heating` additionally requires `current_hvac_mode_normalized == 'heat_cool'` (`test_vacation_retained_setpoint_does_not_latch`). **ACCEPTED-DOCUMENTED (P3)** for the same-target sibling case: bounded by `warmup_max_minutes` + 0.5 °C, unreachable on the shipped instance (its same-target slots never share a day-and-time), a true fix needs per-slot state; stated in the `comfort_floor_delta` description, spec §4.1 and pinned by `test_sibling_slot_with_same_target_shares_latch` |
| B20260907-172334-02 | P2 | high | failure | R2-003 | STEP 3 `stop` | the single `temp_lost` edge is swallowed when the climate entity is unavailable at that moment (hard stop precedes the sensor step) | none | **FIXED** — the `temp_lost` push is also delivered from the climate-unavailable branch, before the `stop` (`test_outage_pushes_are_edge_gated`) |
| B20260907-172334-03 | P2→P3 | high | boundary | R2-001 | STEP 2 `today_at` | cross-midnight windows end at midnight (weekday/date re-anchor on the new day) | design-time adjudication: documented limitation (spec §4.1, input descriptions, requirements); all four live slots end before midnight | **ACCEPTED-DOCUMENTED** — plus R1-02's wording on the four `*_hold_until` descriptions and the README, and a post-midnight test row |
| B20260907-172334-04 | P2→P3 | high | latency | R2-004 | STEP 7 edge | a delayed cloud acknowledgement repeats the warmup push | re-raise of design board 20260907-161235 B-11 (R2-005): Tuya pushes the attribute within seconds on this device; the repeated call is idempotent; a pending-command tracker is over-engineering for a blueprint | **ACCEPTED-DOCUMENTED** (down-ranked per the recorded adjudication) |
| B20260907-172334-05 | P3 | high | documentation | R1-02 | `*_hold_until` descriptions, README | descriptions implied an overnight window | none | **FIXED** — "(the slot is evaluated per calendar day, so it still ends at midnight)" ×4 + README; `test_hold_until_anchor_rows` post-midnight row |
| B20260907-172334-06 | P3 | high | documentation | R1-03 | STEP 1 comment, requirements, spec §4.1 | "the deploy step checks each target exists" is not code-backed (the deploy script does no notify lookup) | none | **FIXED** — reworded to "the deploy procedure (plan Task 4 step 2)"; moving the check into `scripts/deploy-blueprint.sh` would be a declared TCB edit for a later session |
| B20260907-172334-07 | P3 | medium | conventions | R1-04 | STEP 5 climate calls | no `continue_on_error` on the two Tuya cloud calls; with boost expiry last, a raising call leaves the helper ON and notifications stale | none | **FIXED** — `continue_on_error: true` on both calls (ventilator parity; the next tick retries); `test_climate_calls_continue_on_error` |

Noise control: 8 leg findings → 7 after cross-leg dedupe (1 cross-family convergence), 0 nits, none below the confidence floor; dismissal memory: B-04 matches the design board's recorded adjudication (down-ranked, not suppressed). Scope: all inside the reviewed diff.

## Per-pillar verification (review-shipped §4.5)

Pillars present: code (`bathroom_heating_rack.yaml`, instance JSON), tests, docs (requirements, README), spec (§4.1 delta), plan (audit record), reviews. Fresh-shell suite 256 → 260 after the fix wave; deploy dry-run ok (18 inputs); HA `validate_config` valid; no secret-shaped strings or TODO/print residue in code files; explicit-path commits throughout; untracked files in the checkout (`.stack-b`, `reviews/board-20260907-163522.md`) belong to other sessions and are outside this diff. Not run on backlog/horizon/convention pillars — none touched.

Report generated 2026-09-07T17:46:40Z.

## Scoped re-review of the fix wave (Sonnet, af315de..8125c6d)

E1–E6, T1–T6, D1–D2: all ADDRESSED (each hunk cited; `test_vacation_retained_setpoint_does_not_latch` hand-traced: mode `off` → `ma_heating` False / strict edge; mode `unknown` → `heat_cool` → True / latched 05:45). New breakage: none. Cosmetic: a test comment says "auto_start 06:35 for ΔT 0.7" where the arithmetic gives 06:32 — assertion unaffected; deferred. Verdict: all findings addressed → BOARD_VERDICT PASS stands.
