## Board Review 20260909-151815
- mode: code-time
- stakes: standard  |  trigger: default-up — automation logic in bedroom_precool.yaml + tests + instance JSON + docs; no hard trigger (router [0], Entry A, epic #25 / session #26, PR #29)
- legs: R1=ran (7 findings: 1×P0 1×P1 3×P2 2×P3; claude-opus-5 via board-reviewer, 648 s, 22 tool uses) R2=ran (6 findings: 2×P1 4×P2; codex-cli 0.153.4, gpt-6-astra, reasoning high, stdin prompt 137 KB) R3=n/a (standard dial — R3 auto only at high-risk) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE=off)
- quorum met: yes
- degraded: no
- base SHA: 2822409 (origin/main, includes PR #28 pre-cool v1.1.0 + LG v1.3.0)  |  head: 3049737
- leg_bundles: {R1:5db75ba1e55d16ad2e0f56f88307f54edd8fecd14f38d46acf66aac69769d307, R2:5db75ba1e55d16ad2e0f56f88307f54edd8fecd14f38d46acf66aac69769d307}   # canonical (§5.2); re-hash of the dispatched bundle.txt matched at 3a
- main_bundle_sha: 5db75ba1e55d16ad2e0f56f88307f54edd8fecd14f38d46acf66aac69769d307   # informational (diff of 5 files vs origin/main + the spec as context)
- tcb_manifest_sha: 294e5c97c5ac2afffe1100b83cc41aa99f3c7b6805edb572a2e3fffaf0a63aea (verify rc=0 at [6])
- finder_rounds: [13]   # standard dial: 1 round (R1 7 + R2 6)
- synthesis_rounds: 1
- call_budget: 4/24 (cycle 1 R1+R2, cycle 2 R1+R2)
- cycle: 2 (CYCLE=2, fix_cycles=1) — cycle 1 {bundle 5db75ba1e55d16ad2e0f56f88307f54edd8fecd14f38d46acf66aac69769d307, base 2822409..3049737}; cycle 2 delta {parent_bundle_sha 5db75ba1e55d16ad…, delta_bundle_sha ccfe9da90fb0ae6e8657ce9bb8f2fd7b3829309c09091da0c3efc402d9b2a702, fix diff 3049737..d032a53, preflight OK files=4, fresh probe, TCB verify rc=0}; delta legs = fresh cold-restart R1 (Opus) + R2 (Codex) seeded with the cycle-1 findings + the fix diff
- operator_ack: none
- pillars present: code (blueprint, tests, instance), spec/plan (addenda), docs (requirements, README), reviews (design-board report); verification-before-completion run on the code pillar: suite + dry-run + constraint greps at 3049737 (see below); spec/plan/docs pillars verified by the task reviews and the integration review

## Cycle 1 — VERDICT: FAIL (open P0: 1, open P1: 3 at synthesis) → fix wave (Task 7, single commit) → cycle 2 delta re-review

Empirical check (orchestrator, admissible, hard rule 5): rendering `lock_ts` → `earliest_turn_on_ts` → `manual_off` → `guard_due` on the cycle-1 artifact at 2026-09-10 01:00 with the fan-only lock's own off stamped 2026-09-09 19:29:30 gives `lock_ts` = tonight's 19:29, `manual_off` = True, `guard_due` = False (RED) — confirms R1-01 ≡ R2-04 at P0.

| id | sev | conf | lens | leg | finding | disposition |
|---|---|---|---|---|---|---|
| R1-01 ≡ R2-04 | P0 | high | correctness / boundary | converged(R1,R2) cross-family + RED empirical | `lock_ts` has no overnight day-shift: from midnight to wake it points at TONIGHT's lock, so the fan-only lock's own off (yesterday 19:29) escapes the exemption, `manual_off` flips true, the night guard is dead 00:00→wake on every fan-only night, and STEP 7e raises a false "Manual Override" notice 00:00–01:00. | ACTION F1: `lock_ts` anchored to the night's start date like `earliest_turn_on_ts`; rendered chain tests at 19:35, 00:30 and 01:00 incl. a person's 23:00 off. |
| R1-02 (+R1-03) | P1 | high | correctness / tests | R1 | `settle_setpoint_due`'s ± correction_step band fires on the QUANTISED deep-night nudge (int(19.5) = 19, |19−21| = 2.0 > 1.6) → STEP 6b vs DEEP_NIGHT_CHECK alternate `set_temperature` every tick while the windows overlap; the guarding test pinned 19.5, a value the unit cannot hold. | ACTION F7: `night_hold_setpoints` (maintaining + quantised nudges) / `setpoint_is_night_hold`; settle corrects only a setpoint outside that set (stale DRIVE 18 still corrected); tests derive the nudge by rendering. |
| R2-01 | P1 | high | safety / TOCTOU | R2 | The live re-check cannot cancel a `turn_on` the Tuya cloud applies 10–60 s later; if the PIR turns on inside that lag while the fan is still off, the cutoff's `to: on` trigger has passed and nothing cuts the fan. | ACTION F3: `fans_unsafe_on` (interlocked fan on < 120 s while an interlock reads `on`) → `fan.turn_off` first in STEP 5c, every tick; tests. |
| R2-02 | P1 | high | safety | R2 | A bare `turn_on` can restore `heat`; with a stale cloud `off` the settle never runs and a heater stays on. | ACTION F2: guard tick = `turn_on` + unconditional `set_hvac_mode desired_mode` (2 beeps); settle keeps setpoint/fan (+ mode for manual night starts); docs numbers. |
| R2-05 | P2 | high | override | R2 | `ac_started_ts` (climate `last_changed`) also moves on cool→dry and at restart, reopening fan assist with a new reference against a manual off. | ACTION F4: assist window also requires > 600 s since `automation_up_since_ts`; mode-transition residual documented (dry + fan_assist both default off). |
| R2-06 | P2 | high | testing | R2 | The fan-step test pins tokens, not behaviour (`or true` would pass); step order pinned via comments. | ACTION F5: rendered live re-check test (PIR on / cleared 2 min / touched / at target / missing / clean) + parsed action-order assertion. |
| R1-04 | P2 | high | tests | R1 | `_manual_off` fixture renders a 19:30 off at now = 17:00 (impossible), so the post-midnight branch was never exercised. | ACTION F1 tests (explicit `now` per case). |
| R1-05 | P2 | high | conventions | R1 | The dependency-order pin was not extended for `lock_ts` before `earliest_turn_on_ts` and `fan_only_mode` before `manual_off` (an Undefined `lock_ts` would silently read 0.0). | ACTION F8. |
| R2-03 | P2 | high | override | R2 | A person's off inside `[lock_ts, lock_ts+180)` on a fan-only night when the lock did NOT switch the unit off is misread as ours; the next 5-min guard tick may bring the unit back once. | ACCEPTED, documented (F6): bounded to one beep, self-healing (a second manual off is respected); stateless ownership is not available. Cross-family raised, orchestrator-adjudicated by invariant analysis; kept in the report at P2. |
| R1-06 | P3 | high | docs | R1 | Requirements formula blocks still show `ideal_temp` in `delta_in`, `cooling_needed`, `bedtime_error`. | ACTION F9. |
| R1-07 | P3 | medium | logic | R1 | STEP 5's AC-unavailable / no-sensor stops precede STEP 5c and the STEP 7f notice, so a cloud outage across the settle window skips every fan write AND the "reported once" notice. | ACTION F10: STEP 5c (with the notice folded in) moves between STEP 4 (vacation stop) and STEP 5; F3's cut therefore runs every tick. |

Nits: 0. Dismissal memory: none matched. All P0/P1 actioned in-session (§4.5.1); cycle 2 = fresh cold-restart delta legs (R1 Opus, R2 Codex) seeded with the finding text + the fix diff, its own preflighted bundle.

## Cycle 2 (delta legs on 3049737..d032a53, bundle ccfe9da9…) — VERDICT: FAIL (open P1: 1 new converged + 2 operator residuals) → fix wave 2 (Task 8) → cycle 3 (cap)
- legs: R1=ran (8: 1×P1 3×P2 4×P3; claude-opus-5 cold-restart, 580 s) R2=ran (5: 2×P1 3×P2; gpt-6-astra cold-restart, stdin 100 KB)
- Cycle-1 findings re-verdicted: R1-01≡R2-04 (P0) ADDRESSED (both legs; orchestrator GREEN render at 01:00: manual_off False, guard_due True; person's 23:00 off still True) · R1-02/R1-03 ADDRESSED (settle vs quantised nudge: 19/21/22 untouched, 18/24 corrected) with a test-coupling P2 (R1C2-03) · R2-02 ADDRESSED in mechanism (turn_on + set_hvac_mode) with residual R2C2-03 · R2-05 partly (R2C2-02, R1C2-04) · R2-06 ADDRESSED · R1-04/05/06/07 ADDRESSED with P3 doc/test follow-ups (R1C2-05/06/07) · R2-03 wording contested (R2C2-04).

| id | sev | conf | lens | leg | finding | disposition |
|---|---|---|---|---|---|---|
| R1C2-01 ≡ R2C2-05 | P1 | high | correctness / override | converged(R1,R2) cross-family | The new `fans_unsafe_on` cut runs on every tick and re-cuts a person's deliberate `on` every minute while the sensor reads on (the 120 s grace is unreachable on a 1-min tick); it keys on `last_updated`, so a speed change on a long-running fan is cut too; it iterates `interlocked_fans`, not `bedroom_fans`. | ACTION G1: the cut can only cancel this blueprint's own in-flight write — gated on `in_fan_settle or in_fan_assist_window`, fan `last_changed` newer than the window's reference and < 120 s, interlock `on`, `bedroom_fans ∩ interlocked_fans`. Rendered tests (8 cases). |
| R2C2-01 | P1 | high | safety | R2 (residual of R2-01) | The cut reacts only after the delayed `turn_on` lands: a fan can run under an adult for ≤ one tick + delivery latency (≈ 1–2 min at 1 %), and cutting it does not restore a cutoff resume that write cancelled. Codex's recommendation: no direct fan writes to the shared fan until a cutoff-owned command path exists. | **ADJUDICATED BY THE OPERATOR 2026-09-09 (Martin): "Both fans, accept the residual."** The finding stands as a real, bounded exposure — a late Tuya write can leave a fan at 1 % under an adult for ≈ one tick plus delivery latency, and the cut cannot restore a cutoff resume that write cancelled — and is recorded, not killed (security/safety findings never die on votes; §5.3 default-keep). Martin weighed it against the interlock + clear-hold covering every ordinary case and the 1 % exposure level. Documented as an accepted residual in the requirements + spec, with the one-line escape hatch (drop `fan.ceiling_fan_light_v2` from the instance's `bedroom_fans`). |
| R2C2-03 | P1 | medium | containment | R2 (residual of R2-02) | If `turn_on` restores heat AND `set_hvac_mode` fails AND the cloud keeps reporting `off`, no observer can act; heat persists between 5-min retries. | MITIGATED (G3 heat backstop: a running `heat` at night in fan_only is switched off within a tick; worst case ≤ 1 min heat per 5-min cycle when the mode set keeps failing) — the unobservable stale-`off` residual stays OPEN for the operator (same class as v1.0's PRECOOL turn_on → set_hvac_mode exposure). |
| R2C2-02 ≡ R1C2-04 | P2 | high | override | converged(R2,R1) | The 600 s restart grace only delays a re-opened assist window; `ac_started_ts` also re-stamps on an `unavailable → cool` flap or a mode change. | ACTION G2: restart inside today's window disables assist for the day; flap/mode residual documented honestly (fan_assist + dry default off). |
| R1C2-03 | P2 | high | tests | R1 | `night_hold_setpoints` re-implements the quantise/clamp of `deep_target_setpoint` without the membership pin the repo uses for `known_setpoints`. | ACTION G6. |
| R2C2-04 | P2 | high | docs | R2 | The accepted lock-window misread was documented as "one beep, self-healing"; a second off inside the window is still exempt, and a guard trip is now two commands. | ACTION G4 (honest wording). |
| R1C2-05 / R1C2-06 | P3 | high | docs | R1 | Two leftover "one-beep guard" sentences; the settle sentence still describes the ± band. | ACTION G7. |
| R1C2-07 | P3 | medium | tests | R1 | Nothing pins that the fan-skipped notice now survives an AC-unavailable tick. | ACTION G8. |
| R1C2-08 | P3 | high | conventions | R1 | `fans_unsafe_on` iterated `interlocked_fans` instead of `bedroom_fans`. | ACTION G1. |

- call_budget: 4/24 after cycle 2; cycle 3 adds 2.

## Cycle 3 (the cap) — delta legs on d032a53..57b1d5d, bundle c831bf1f… — VERDICT: FAIL → findings actioned in-session (Task 9), NO fourth convening (CYCLE cap 3, §4.5)
- legs: R1=ran (6: 1×P1 3×P2 2×P3; claude-opus-5 cold-restart, 538 s) R2=ran (4: 3×P1 1×P2; gpt-6-astra cold-restart)
- TCB at cycle 3: `~/.claude/settings.json` changed mid-chain (operator's `/model` default save + this session's permission approvals; the chain never edits it). Declared via `~/AI/reviews/declared-tcb-e47dda9f8b1e8bcb.txt`; `verify baseline declared` → rc 0, zero drift lines. Baseline itself NOT rewritten — declared changes re-baseline at [7] merge per §7.10.
- Cycle-2 findings re-verdicted: R1C2-01 ≡ R2C2-05 ADDRESSED in mechanism (the cut is window-bounded now) with two precision residuals (R1C3-03 ≡ R2C3-01 gates, R1C3-04 ≡ R2C3-04 edge) · R2C2-02/R1C2-04 ADDRESSED · R2C2-03 mechanism ADDRESSED but the mitigation introduced R1C3-01 · R1C2-02/03/05/06/07/08 ADDRESSED · R2C2-04 ADDRESSED (wording corrected again in H8).

| id | sev | conf | lens | leg | finding | disposition (Task 9) |
|---|---|---|---|---|---|---|
| R1C3-01 ≡ R2C3-03 | P1 | high | correctness | converged(R1,R2) cross-family | The heat backstop's own `climate.turn_off` moves the climate `last_changed`, so `manual_off` reads True for the rest of the night and `guard_due`'s `not manual_off` kills the night guard until wake; STEP 7e then blames a person in a notice. Introduced by the cycle-2 mitigation itself. | H1: the backstop no longer switches the unit off — it corrects the mode (`set_hvac_mode desired_mode`, idempotent, nothing latches). |
| R1C3-03 ≡ R2C3-01 | P2/P1 | high | correctness / override | converged(R1,R2) cross-family | `fans_unsafe_on` is armed by the raw windows while the write lists also require `night_fans_tonight` / `fan_assist_tonight`; with the deployed `fan_assist: off` the cut is armed all evening though no write is possible, and would cut a person's deliberate start — the case the safety cutoff deliberately leaves to the person. | H4: gate on the same enable+window pairs the write lists use; `ref_ts` from the same branch. |
| R1C3-04 ≡ R2C3-04 | P2/P1 | high | correctness / docs | converged(R1,R2) cross-family | A write issued on the last in-window tick lands after the window closes, so the next tick renders `[]` and the fan is never cut — the accepted-residual wording ("cuts it on the next tick") overstates the bound. | H5 (120 s grace past each window end, same references) + H8 (honest wording, 1 % night vs 21 % assist, best-effort not guaranteed). |
| R2C3-02 | P1 | high | containment | R2 | The backstop sat behind STEP 5's sensor/AC validation stops, so a dead bedroom sensor prevented the correction while the unit heats. | H3: moved to STEP 5d, immediately after the fan step and before the validation stops. |
| R1C3-02 | P2 | high | logic | R1 | On a heat read inside the settle window both `settle_mode_due` and `heat_backstop_due` fire — two commands for one repair, uncounted in the documented bound. | H2: the backstop is the single owner of the heat case (`settle_mode_due and not heat_backstop_due`). |
| R1C3-05 | P3 | high | docs | R1 | The `night_mode` input description (the text shown in the HA editor) still promised "one beep". | H6. |
| R1C3-06 | P3 | high | conventions | R1 | The definition-order pin does not cover `fans_unsafe_on`'s four new upstream dependencies; an unordered `ref_ts | float` would silently be 0.0 and cut every fan on within 120 s. | H7. |
| R2C3-01's ownership objection (residual) | — | — | override | R2 | Codex holds that window membership is not command ownership: inside an armed window a person's deliberate start under an active sensor is still cut, and each retry refreshes the clock. | OPEN, RECORDED. Bounded by H4 to the windows in which this blueprint actually writes (with the deployed instance: the 45 min after the bedtime lock on nights `night_fans` is on). The safer direction is cutting; the operator has accepted the fan-writes-to-the-shared-fan class of risk (R2C2-01). |
| R2C3-02's "one minute" wording | — | — | docs | R2 | The heat bound should be described as a nominal observation-to-request interval, not a guaranteed heating duration. | H8 wording. |

**Cap reached.** `CYCLE=3` (derived `fix_cycles=2`); a fourth convening is not justifiable under §4.5. All P1/P2 findings are actioned in-session by Task 9; the two residual objections above are recorded for the operator. Halt artifact: `reviews/halt-multi-round-20260909-151815.md`.
