## Board Review 20260907-163522
- mode: code-time
- stakes: standard  |  trigger: default-up — blueprint YAML + instance JSON + test; no hard trigger (router [0], Entry D)
- legs: R1=ran (7 findings; claude-opus-5 via board-reviewer) R2=ran (0 findings; codex-cli 0.153.4, account default model gpt-6-astra — no `-m` pin per Martin's 2026-09-05 policy) R3=n/a (standard dial) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE=off; hotfix)
- quorum met: yes
- degraded: no
- base SHA: 796f1b7 (vs origin/main 0b7d02f)
- leg_bundles: {R1:22628def7bebd321e2ff29dc5b10bf40792fc376b3b5725087ba2e24606310ca, R2:22628def7bebd321e2ff29dc5b10bf40792fc376b3b5725087ba2e24606310ca}   # canonical; re-hash of each dispatched payload matched at 3a
- main_bundle_sha: 22628def7bebd321e2ff29dc5b10bf40792fc376b3b5725087ba2e24606310ca
- finder_rounds: [7]   # standard dial: 1 round
- synthesis_rounds: 1
- cycle: 1 (CYCLE=1, fix_cycles=0)
- panel: {votes: none, framings: none, empirical_checks: 1 (R1-06 live state read), inquorate: no} — no P0/P1 surfaced, so §5.3 adjudication was not triggered
- tcb_manifest_sha: adcf6c71177b38a12961876e49aca2381ff47a63d84a123195d4fd8b7059f79d (verify rc=0 at [6] and at Phase 0c)
- call_budget: 2/24
- operator_ack: none (non-hard-trigger Entry D; ships on the operator approval artifact recorded on issue #15)
- deterministic pre-checks (3b): pytest 138 passed; deploy --dry-run green for both instances; live traces finished post-deploy; no finding contradicted by a passing test
- confidence caveat: R2 returned an empty findings array after ~60 s of runtime on the default model; treated as a valid clean result per the LEG CONTRACT, but single-leg findings are capped at medium (no cross-family convergence available on an empty R2).
- note: R2+R3 are same-family (both Codex) — their convergence counts below cross-family; the Claude-reviews-Claude self-preference residual is open until a true third family (R4) returns.

## VERDICT: PASS — open_P0: 0 | open_P1: 0 | P2: 2 (should-fix, actioned post-board in the same session) | P3: 5

## Findings (P0 → P3; all single-leg R1, confidence capped at medium)
```json
{
 "finding_id": "B20260907-163522-R1-01",
 "severity": "P2",
 "confidence": "medium",
 "lens": "test-validity",
 "source_leg": "R1",
 "path": "tests/test_bedroom_precool_structure.py",
 "section": "test_instants_are_carried_as_timestamps_not_datetimes",
 "evidence": "assert not re.search(r\"\\b\\w+_dt\\b\", text), \"datetime-typed variables must not cross steps\"",
 "refutation_status": "none",
 "class": "Important",
 "recommended_fix": "The regression pin is NAME-shaped and literal-shaped, not semantics-shaped, and there is not one realized-output assertion in the file. `\\b\\w+_dt\\b` only bans the old identifier suffix: reintroducing the exact bug as `turn_on_at: \"{{ today_at(bedtime) - timedelta(minutes=lead|int) }}\"` consumed by `turn_on_at.hour`, or as the ORIGINAL bedtime_dt failure mode (a datetime compared with `<=` across a variables boundary, which is not a `.strftime(` call at all), passes every one of the five new tests green. The sibling suite tests/test_lg_ac_climate_structure.py already ships a render harness (`render_var(bp, name, ctx)` with `env.globals[\"as_timestamp\"]`) and exact-equality AST pins via `get_var(bp, name)`; this file uses neither. Add at least one rendered assertion per headline property using that harness: render `turn_on_ts` -> feed the result into `turn_on_tod` -> assert the local `HH:MM:SS` string, and render `forecast_window_temps` with a stub forecast list plus a float `bedtime_ts` to prove the window filter selects and does not raise. Replace the `_dt` name ban with a type-shaped check (e.g. assert every action-level `variables:` value that is later consumed by a filter/comparison renders to a literal-evaluable scalar).",
 "orchestrator_disposition": "ACTION post-board (should-fix): add rendered assertions via the sibling render harness; keep the lexical pins as a floor."
}
```
```json
{
 "finding_id": "B20260907-163522-R1-02",
 "severity": "P2",
 "confidence": "medium",
 "lens": "correctness",
 "source_leg": "R1",
 "path": "bedroom_precool.yaml",
 "section": "STEP 2c comment above now_tod (changed hunk @@ -680,7 +682,7 @@)",
 "evidence": "# carried as a same-day timestamp purely for the debug dump. ---",
 "refutation_status": "none",
 "class": "Important",
 "recommended_fix": "The comment is materially false and the diff edited this exact line (datetime -> timestamp) while preserving the false claim. turn_on is NOT debug-only: `turn_on_tod` is consumed 13 lines later by `precool_started: \"{{ (cooling_needed and now_tod >= turn_on_tod) or ac_is_running }}\"`, which drives in_precool_window / in_day_off and therefore the whole `phase` dispatch in STEP 6. Since the incident under repair was caused by exactly this variable's cross-boundary type, leaving a comment that tells the next maintainer it is inert is the setup for the next regression (a format change, rename, or deletion of turn_on_tod would silently move the PRECOOL start boundary). Rewrite the comment to state that turn_on_ts is the moving boundary carried as a UNIX timestamp and that turn_on_tod is load-bearing for precool_started, used for the debug dump only incidentally.",
 "orchestrator_disposition": "ACTION post-board (should-fix): rewrite the comment — turn_on_tod is load-bearing for precool_started, not debug-only."
}
```
```json
{
 "finding_id": "B20260907-163522-R1-03",
 "severity": "P3",
 "confidence": "medium",
 "lens": "correctness",
 "source_leg": "R1",
 "path": "bedroom_precool.yaml",
 "section": "STEP 2c turn_on_tod / lock_tod / deep_end_tod",
 "evidence": "turn_on_tod: \"{{ turn_on_ts | float | timestamp_custom('%H:%M:%S') }}\"",
 "refutation_status": "none",
 "class": "Nit",
 "recommended_fix": "The fix silently changed turn_on_tod's semantics from wall-clock rendering to an instant round-trip, and the two sibling boundaries in the same variables block were left on the old semantics: `lock_tod` and `deep_end_tod` still render `(today_at(...) +/- timedelta(...)).strftime('%H:%M:%S')` directly off the datetime. `today_at(x) - timedelta(...)` is naive wall-clock arithmetic on a zoneinfo-aware datetime, so on a spring-forward day a result landing in the skipped local hour resolves through `as_timestamp` with the pre-transition offset and `timestamp_custom(local=True)` renders it one hour LATER than `.strftime()` would have (e.g. bedtime 04:00 with lead 90 -> old '02:30:00', new '03:30:00'), shifting the PRECOOL start boundary on that one day. Unreachable at the deployed config (bedtime 19:30, lead cap 240 min keeps turn_on >= 15:30) but the blueprint is published for import via the README badge. Either convert lock_tod/deep_end_tod to the same `as_timestamp(...) | timestamp_custom(...)` form so all four boundaries share one semantics, or add a comment recording that turn_on_tod is instant-derived while its siblings are wall-clock-derived.",
 "orchestrator_disposition": "NOTE: comment the semantic split (turn_on_tod instant-derived; lock_tod/deep_end_tod wall-clock); unreachable at the deployed config (bedtime 21:30, lead cap 240). Converting the siblings is deferred to a normal session — outside hotfix scope."
}
```
```json
{
 "finding_id": "B20260907-163522-R1-04",
 "severity": "P3",
 "confidence": "medium",
 "lens": "conventions",
 "source_leg": "R1",
 "path": "tests/test_bedroom_precool_structure.py",
 "section": "test_precool_instance_keys_exist_and_weather_supports_hourly_forecasts / test_lg_ac_instance_escalation_stages_are_ordered_and_in_range",
 "evidence": "unknown = set(inst[\"use_blueprint\"][\"input\"]) - set(inputs)",
 "refutation_status": "none",
 "class": "Nit",
 "recommended_fix": "Both instance tests hand-roll a strict SUBSET of validation the repo already owns. `scripts/deploy-blueprint.sh --dry-run` validates unknown keys, `required inputs missing`, `use_blueprint.path`, and the `^[A-Za-z0-9_-]+$` instance-id rule, and tests/test_deploy_blueprint_script.py already ships the `run(*args)` subprocess harness for invoking it. The reimplementation drops the required-input check -- the failure the script itself labels 'would make the instance unavailable' -- and the LG test additionally drops the `use_blueprint.path` check the precool test keeps. Replace the hand-rolled subset with a call to the dry-run on the two committed instance JSONs (`run('--dry-run', str(BP_PATH), 'leviemartin/bedroom_precool.yaml', str(PRECOOL_INSTANCE))` and the LG equivalent, asserting returncode 0), and keep only the genuinely new assertions (weather_entity value, alias version, s1 < s2, selector range) as Python.",
 "orchestrator_disposition": "DEFER to a normal session: replace the hand-rolled subset with a deploy --dry-run call (the dry-run already ran green for both instances in this session)."
}
```
```json
{
 "finding_id": "B20260907-163522-R1-05",
 "severity": "P3",
 "confidence": "medium",
 "lens": "conventions",
 "source_leg": "R1",
 "path": "deploy/lg_ac_climate_1775578219942.json",
 "section": "alias",
 "evidence": "\"alias\": \"LG AC Climate Control v1.0.0\",",
 "refutation_status": "none",
 "class": "Nit",
 "recommended_fix": "This diff establishes and test-pins the convention that a deploy instance alias tracks its blueprint version (`assert inst[\"alias\"].endswith(\"v1.0.1\")` against `bp[\"blueprint\"][\"name\"].endswith(\"v1.0.1\")`), then commits an LG instance whose alias is frozen at v1.0.0 while lg_ac_climate.yaml is `name: \"LG AC Climate Control v1.2.0\"` -- two minor versions of drift, and the new LG test asserts nothing about the alias. Because deploy-blueprint.sh POSTs the whole config, the automation is literally named v1.0.0 in HA while running blueprint v1.2.0, which misleads during incident triage. Either bump the LG alias to v1.2.0 in the same deploy (note this changes the friendly name only -- the entity_id is pinned by the unchanged unique_id `1775578219942`), or add a shared test asserting alias-version == blueprint-version for every committed deploy/*.json with an explicit, commented exemption if the drift is deliberate.",
 "orchestrator_disposition": "OPERATOR DECISION: LG instance alias v1.0.0 vs blueprint v1.2.0 — friendly-name-only change; not part of the approved diffs, ask Martin."
}
```
```json
{
 "finding_id": "B20260907-163522-R1-06",
 "severity": "P3",
 "confidence": "medium",
 "lens": "logic",
 "source_leg": "R1",
 "path": "deploy/bedroom_precool_1779553673971.json",
 "section": "use_blueprint.input.outdoor_temp_sensor",
 "evidence": "\"outdoor_temp_sensor\": \"sensor.openweathermap_temperature\",",
 "refutation_status": "auto-refuted (live state)",
 "class": "Nit",
 "recommended_fix": "The remediation is partial against its own premise. The change moves `weather_entity` off `weather.openweathermap` because that integration's weather platform is non-functional here, but leaves the SAME integration as the sole source of `outdoor_now`, and that path fails silently: `outdoor_now: \"{{ outdoor_now_raw | float(ideal_temp | float) }}\"` substitutes ideal_temp on an unavailable sensor, which drives `delta_out` to 0 and `forecast_max` to at least ideal_temp, shrinking `lead` on exactly the hot evenings the blueprint exists for. The only operator signal (STEP 7b) additionally requires `phase == 'DAY_OFF'` AND an empty forecast window, so a degraded sensor with a healthy Met.no forecast is invisible. Confirm `sensor.openweathermap_temperature` is reporting (not `unavailable`) in the live instance, and if the integration is degraded repoint outdoor_temp_sensor at a Met.no-derived sensor in the same deploy. Note the LG instance's `\"weather_entity\": \"weather.openweathermap\"` was correctly left alone -- lg_ac_climate.yaml only reads `state_attr(entity_weather, 'temperature')` and never calls weather.get_forecasts.",
 "orchestrator_disposition": "Live check 16:38Z: sensor.openweathermap_temperature = 23.35 °C, updating — the OWM SENSOR platform works; only the weather-forecast service is unsupported. Monitoring gap noted, no defect."
}
```
```json
{
 "finding_id": "B20260907-163522-R1-07",
 "severity": "P3",
 "confidence": "medium",
 "lens": "pre-existing",
 "source_leg": "R1",
 "path": "bedroom_precool.yaml",
 "section": "STEP 2b forecast_daily_fallback (lines 614-620, adjacent to the changed hunk)",
 "evidence": "{% set d = state_attr(weather_entity, 'forecast') %}",
 "refutation_status": "none",
 "class": "Pre-existing",
 "recommended_fix": "Pre-existing, two lines below the changed comparison, but it directly weakens the prediction path this hotfix just brought to life. The `forecast` attribute on weather entities was deprecated in HA 2023.9 and removed in 2024.4 in favour of weather.get_forecasts; on HA 2026.9 `state_attr(weather_entity, 'forecast')` is always None, so the `d is iterable and d is not string and d | length > 0` branch is dead and forecast_daily_fallback unconditionally collapses to `outdoor_now`. Combined with `forecast_fetch_due` gating the real fetch to minute % 15 == 0, `forecast_max` equals outdoor_now on 14 of every 15 ticks with no daily-forecast backstop at all. Either delete forecast_daily_fallback and fold its `else` branch into forecast_max (three lines saved, identical behaviour), or replace it with a `type: daily` weather.get_forecasts call so the fallback is real.",
 "orchestrator_disposition": "Pre-existing, outside the changed hunks (deprecated `forecast` attribute fallback). Follow-up session."
}
```

## R2 (Codex) — `{"leg":"R2","findings":[]}` (validator: ran, 0 findings)

## Pillars / verification (review-shipped §4.5)
Pillars present: code (blueprint YAML + instance JSON), tests. Verification-before-completion run per pillar: tests re-run fresh from a clean shell (138 passed, rc 0); acceptance criteria from issue #15 checked against live traces (16:32:00Z + manual 16:32:44Z `finished`, `bedtime_ts`/`turn_on_ts`/`turn_on_tod`/`phase` rendered; LG `esc_stage_1=20`, `esc_stage_2=40`), error log clean after 16:30Z. Not run on spec/plan/audit/backlog/horizon/convention pillars — none touched (Entry D has no plan).

## Post-board actions (same session, verdict PASS → should-fix P2s actioned; no delta cycle required — no P0/P1, fix diff stays inside the reviewed manifest)
- fd2af13 (pushed): R1-02 comment rewritten (turn_on_tod load-bearing; lock_tod/deep_end_tod wall-clock vs turn_on_tod instant-derived noted → also closes R1-03 as a documented split); R1-01 rendered boundary tests added (`_reparse` = ast.literal_eval models HA's variables boundary): turn_on_ts → float, turn_on_tod "18:16:00" for bedtime 21:30 / lead 194 (float and string hand-over), bedtime_ts rolls to tomorrow after bedtime, forecast window filters by re-parsed bedtime_ts. RED-proven: all three fail on the original v1.0.0 (KeyError) and the turn_on_tod test fails on a same-class mutant (`turn_on_ts.strftime`, UndefinedError). Suite 141 passed.
- Redeployed the comment-only YAML change at 16:50Z (blueprint/save ok, instance reloaded, automation on) so HA's copy equals the merged file.
- R1-04 (dry-run reuse in tests), R1-07 (pre-existing deprecated `forecast` attribute fallback): deferred to a normal session — outside hotfix scope; recorded here and on issue #15.
- R1-05 (LG instance alias v1.0.0 vs blueprint v1.2.0): operator decision pending (friendly-name-only change).
- R1-06: auto-refuted by live state (sensor.openweathermap_temperature reporting 23.35 °C at 16:38Z).
- Rollback record: pre-change live instance configs saved at ~/AI/reviews/board-20260907-163522-live-configs-before.json; deploy/<id>.prev.json in the worktree now holds the post-fix configs (second deploy overwrote the first backup).
