## Board Review 20260908-071936
- mode: code-time
- stakes: standard  |  trigger: default-up — blueprint YAML + instance JSON alias + test + requirements doc; no hard trigger (router [0], Entry D)
- legs: R1=ran (5 findings; claude-opus-5 via board-reviewer, 494 s) R2=ran (1 finding; codex-cli 0.153.4, account default model gpt-6-astra, reasoning high, 23 s — no `-m` pin per Martin's 2026-09-05 policy) R3=n/a (standard dial) R4=reserved PonyTail=skipped (PONYTAIL_DEFAULT_MODE=off; hotfix)
- quorum met: yes
- degraded: no
- base SHA: c2ac118 (vs origin/main 224892b)
- leg_bundles: {R1:20331bb56fb8b4de8b3a76fb7ed97a6114df4148104ce1f4e5be239b9f4bc514, R2:20331bb56fb8b4de8b3a76fb7ed97a6114df4148104ce1f4e5be239b9f4bc514}   # canonical; re-hash of the dispatched bundle.diff matched at 3a
- main_bundle_sha: 20331bb56fb8b4de8b3a76fb7ed97a6114df4148104ce1f4e5be239b9f4bc514
- finder_rounds: [6]   # standard dial: 1 round (R1 5 + R2 1)
- synthesis_rounds: 1
- cycle: 1 (CYCLE=1, fix_cycles=0)
- panel: {votes: none, framings: none, empirical_checks: 2 (R2-01/R1-02 reproduced RED via the test harness + HA /api/template; R1-05 alias claim refuted by a live config read), inquorate: no} — no P0/P1 surfaced, so §5.3 adjudication was not triggered
- tcb_manifest_sha: adcf6c71177b38a12961876e49aca2381ff47a63d84a123195d4fd8b7059f79d (verify rc=0 at [6] entry / Phase 0c)
- call_budget: 2/24
- operator_ack: none (non-hard-trigger Entry D; ships on the operator approval artifact recorded on issue #22)
- deterministic pre-checks (3b): pytest 274 passed (pre-cool file 14, RED 8 on v1.0.1); deploy --dry-run green; live post-deploy trace 269cba4d… = DAY_OFF → climate.turn_off (realized wake-off RED→GREEN); no finding contradicted by a passing test
- note: R2+R3 are same-family (both Codex) — their convergence counts below cross-family; the Claude-reviews-Claude self-preference residual is open until a true third family (R4) returns. Here the one converged finding IS cross-family (R1+R2).

## VERDICT: PASS — open_P0: 0 | open_P1: 0 | P2: 2 (should-fix, actioned post-board in the same session) | P3: 3

## Findings (P0 → P3; converged first)
```json
{
 "finding_id": "B20260908-071936-R2-01",
 "converged_with": "B20260908-071936-R1-02",
 "severity": "P2",
 "confidence": "high",
 "lens": "validation",
 "source_leg": "converged(R1,R2) — cross-family",
 "path": "bedroom_precool.yaml",
 "section": "STEP 3 configuration validation / STEP 2c earliest_turn_on_tod",
 "evidence": "or earliest_turn_on_tod <= wake_time",
 "refutation_status": "none — reproduced: bedtime 05:00 / wake 04:00 / cap 360 renders earliest_turn_on_tod '23:00:00' and the clause renders False (harness + HA /api/template)",
 "class": "Important",
 "recommended_fix": "The lexical HH:MM:SS compare passes when bedtime − lead_cap wraps past midnight (strftime drops the date), so validation accepts a schedule in which the running-AC latch is false all day and PRECOOL can revert to DAY_OFF. Compare the instants inside ONE template: `(today_at(bedtime) - timedelta(minutes=lead_cap_minutes | int)) <= today_at(wake_time)` → configuration error; pin with a rendered test on the wrap case.",
 "orchestrator_disposition": "ACTION post-board (should-fix): STEP 3 clause replaced by the same-template datetime comparison; test renders the live, boundary and wrap cases."
}
```
```json
{
 "finding_id": "B20260908-071936-R1-01",
 "severity": "P2",
 "confidence": "medium",
 "lens": "logic",
 "source_leg": "R1",
 "path": "requirements_bedroom_precool.md",
 "section": "'Daily State Machine' table + bedroom_precool.yaml STEP 6 DAY_OFF branch",
 "evidence": "| DAY-OFF | wake -> turn_on | Ensure AC off; recompute turn-on each tick | 0 (1 at wake) |",
 "refutation_status": "none",
 "class": "Important",
 "recommended_fix": "The fix makes DAY_OFF's every-tick climate.turn_off reachable, so ANY running unit between wake and bedtime − lead_cap (07:15–15:30 live) is switched off within 60 s with a beep — a manual daytime turn-on no longer survives (v1.0.1 hijacked it as PRECOOL instead). State this in the doc/table and the blueprint comment; if manual daytime use must survive, gate the turn-off on a wake edge or on the unit's last_changed being before today's wake.",
 "orchestrator_disposition": "ACTION post-board (doc): table row + explicit manual-use consequence + comment wording ('any running unit'). The behavioural alternative (respect a unit switched on after wake via last_changed) is a design choice for Martin → recorded on #19 (manual-override handling)."
}
```
```json
{
 "finding_id": "B20260908-071936-R1-03",
 "severity": "P3",
 "confidence": "high",
 "lens": "correctness",
 "source_leg": "R1",
 "path": "tests/test_bedroom_precool_structure.py",
 "section": "PHASE_CHAIN / _render_chain",
 "evidence": "ctx[name] = _reparse(_render(bp, name, now, **ctx))",
 "refutation_status": "none",
 "class": "Nit",
 "recommended_fix": "The chain order is hardcoded, so a YAML reordering that puts earliest_turn_on_tod after precool_started (Undefined at render → per-tick TemplateError) keeps the suite green. Pin the source order.",
 "orchestrator_disposition": "ACTION post-board: text-level order pin added (earliest_turn_on_tod: before precool_started:)."
}
```
```json
{
 "finding_id": "B20260908-071936-R1-04",
 "severity": "P3",
 "confidence": "medium",
 "lens": "pre-existing",
 "source_leg": "R1",
 "path": "bedroom_precool.yaml",
 "section": "STEP 3 config-error stop vs STEP 4 vacation override / STEP 6 DAY_OFF",
 "evidence": "- stop: \"Configuration error\"",
 "refutation_status": "none",
 "class": "Pre-existing",
 "recommended_fix": "Any STEP 3 configuration error (this clause included) halts before the vacation override and the DAY_OFF turn-off, leaving a running unit running with only a notification. Move the safety-off paths ahead of the stop (same class as the sensor-unavailable stop).",
 "orchestrator_disposition": "NOTE → session #19 (pre-cool v1.1.0 hardening: safety-off paths ahead of every stop). Clamping instead of erroring was rejected: clamping earliest_turn_on to wake would re-create the unreachable wake-off this hotfix removes."
}
```
```json
{
 "finding_id": "B20260908-071936-R1-05",
 "severity": "P3",
 "confidence": "medium",
 "lens": "conventions",
 "source_leg": "R1",
 "path": "deploy/bedroom_precool_1779553673971.json",
 "section": "alias bump + blueprint description",
 "evidence": "\"alias\": \"Bedroom Sleep Pre-Cool v1.0.2\",",
 "refutation_status": "partly auto-refuted (live): the deploy script POSTs the instance JSON to /api/config/automation/config/<id>, which applies the alias — live config and friendly_name read 'Bedroom Sleep Pre-Cool v1.0.2' after the 07:18Z deploy",
 "class": "Nit",
 "recommended_fix": "Add a version-history note to the blueprint description like the sibling bathroom_heating_rack.yaml (`**Version: x.y.z** — what changed … History: …`).",
 "orchestrator_disposition": "ACTION post-board (description history line). Alias half refuted by the live read."
}
```

## Post-board actions (same session)
- STEP 3: `earliest_turn_on_tod <= wake_time` (string) → `(today_at(bedtime) - timedelta(minutes=lead_cap_minutes | int)) <= today_at(wake_time)` (same-template instants); message unchanged.
- Blueprint description: v1.0.2 what-changed + History line; STEP 2c comment: "any running unit".
- requirements_bedroom_precool.md: DAY-OFF row + manual-use consequence.
- tests: order pin (R1-03); validation test renders live / boundary / wrap cases from inputs.
- Re-deploy + live re-verify after the fix wave; scoped re-review by the orchestrator (fix diff small, no new hard trigger).

## Post-board re-verify (orchestrator, scoped)
- Fix wave: STEP 3 datetime clause, description history, doc row + manual-use consequence, test order pin + wrap case. pytest 275 passed; deploy --dry-run green; HA /api/template renders the new clause False for the live schedule and True for the wrap case (bedtime 05:00 / wake 04:00 / cap 360).
- Re-deployed live 07:32:23Z; tick 07:33:00Z (trace a51989c9…) `finished`, `phase: DAY_OFF`, `earliest_turn_on_tod: 15:30:00`, `precool_started: false`, 0 service calls (unit off since 07:19:03Z), no config-error notification, 0 log entries. No new hard trigger; manifest unchanged (same 4 files + report).
