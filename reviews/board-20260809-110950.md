# Board Review — 20260809-110950

- mode: design-time | stakes: standard | trigger: default-up, automation logic in lg_ac_climate.yaml
- bundle hash: 049a774744f62d7a364ee817f8dac2ea939dc3d4efb803c27297d61cd6dc127d | base SHA: 7d4a2f7
- files reviewed: spec + plan + v1.0.0 baseline (frozen single bundle, both legs)
- legs: R1=ran (Opus, correctness/logic/consistency) · R2=ran (Codex gpt-5.4, security/edge, read-only sandbox) · R3=skipped · PonyTail=skipped (lens folded into plan discipline)
- quorum met: yes | degraded: yes — ℹ architecture lens (Gemini R3) absent, standing
- rounds: 2 (round 2 = focused re-review of the fix wave)
- deterministic pre-checks: baseline PASS=11 FAIL=0 (nightlight suite) · v1.0.0 YAML parses (3 triggers / 3 action steps)

## Round 1 findings (all actioned in-session — commits d2cc75b spec, 4d7df28 plan)

| id | sev | conf | lens | finding (compressed) | resolution |
|---|---|---|---|---|---|
| R1-1 | P1 | high | logic | Init seed while ThinQ entity still `unavailable` poisons expected → false 60-min hold after every restart | Seed kept unconditional; `unavailable`/`unknown` expected-mode is now a no-expectation **sentinel** ignored by detection (spec rules 5/6) |
| R1-2 | P1 | high | logic | Expected-writes reuse pre-seed STEP-1 `hold_until` → hold survives restart, STEP 2c a near-no-op | All expected-writes now write `hold_until: 0`; only hold-start writes nonzero |
| R1-3 | P1 | high | correctness | Command-then-write not atomic: failed/interrupted `climate.*` call skips expected-write → false hold | `continue_on_error: true` on all comfort `climate.*` calls; residual (one spurious self-clearing hold) documented in spec + degradation matrix; two-strike alternative **rejected** — it reverts a genuine manual change once, defeating the feature |
| R2-1 | P1 | high | security | `hold_until` honored unvalidated — far-future value from any writer suppresses control indefinitely | `hold_active` bounded to `(now, now + hold_minutes]`; out-of-range ⇒ no hold; self-heals on next detection |
| R1-4 | P2 | high | correctness | Partial mapping (`mode` w/o `temp`) → Jinja Undefined passes `is not none`, coerces −99 → false hold | All expected accesses via `.get`; rendered test pins the partial-mapping case |
| R1-5 | P2 | high | consistency | Yesterday-schedule pins vacuous (index-expression only) — copy-paste kills overnight tail silently | Exact-equality pins on both full templates |
| R1-6 + R2-4 | P2 | high (converged) | consistency | `manual_detected` pinned by substrings — inverted predicate ships green | Full exact-equality pin (`MANUAL_DETECTED` constant) + rendered behavior cases |
| R1-7 | P2 | high | logic | Pierce-during-hold strands AC off for the hold remainder; defeats vacation-off instant resume | Design revised: **a safety pierce ends the hold** (writes `hold_until: 0`); spec rule 1 + acceptance (b)(e) |
| R1-8 | P2 | high | consistency | "Non-extending window" false: expiry compares against hold-start snapshot → every tweak restarts window | Hold-active branch now **refreshes expected to live** each run (preserving the clock); expiry resumes cleanly; acceptance (f) |
| R2-2 | P2 | high | edge | Automation reload doesn't fire `homeassistant: start` → stale expected after reload | `automation_reloaded` event trigger added, id `init` (same seed semantics; inert if event name ever wrong); disable/enable residual documented |
| R2-3 | P2 | high | edge | Pending door `for:` countdown lost on restart — exact-time promise overbroad | Carve-out documented (item 6 acceptance + degradation row); 10-min loop is the backstop |
| R1-9 | P2 | med | correctness | Float epoch can exceed default `max:100` input_text; `set_value` overflow silently no-ops → stale expected | Epoch rounded to int in all writes; helper contract mandates `max: 255`; doc now fits even default max |
| R1-10 | P2 | high | convention | Structural pins only; nightlight harness already renders templates — semantic mutations survive | New plan Task 8b: rendered behavior tests for the four load-bearing templates, alongside exact pins |
| R1-11 | P3 | high | consistency | Spec "branches 9–11" off-by-one, misses maintenance branch | Fixed → "8–10 (in-range/maintenance, cool, heat)" |
| R1-12 | P3 | high | consistency | Task 3 "replace the three variables" miscounts (five change; two are additions) | Reworded precisely |
| R1-13 | P3 | high | consistency | `kinds[:3]` prefix pin lets a stray unguarded call hide before the expected-write | Task 8 step 3.6 tightens to whole-sequence equality for comfort + off branches |
| R1-meta | — | — | — | Items 1–8 verified sound (cross-midnight math, Jinja precedence, sequential variables, `is mapping`, `float(none)` weather path, door `for:` restart re-arm, venv/test assumptions); risk concentrated in item 9 | Concurred; item 9 redesigned per above |

Pre-board triple-check findings (also actioned, commits dd6497b/7d4a2f7): helper-JSON mapping
check (P1), door branch by trigger identity (P2), continue-zone idle consequence documented (P2).
Pass 4 cross-check: `feedback_ha_choose_default_catchall_new_trigger` — N/A (ladder has no
`default:`; new triggers intentionally run full state-evaluated passes).

## Round 2 — focused re-review of the fix wave (fresh Opus leg)

Scope: revised spec §9 + plan Tasks 6/7/8/8b only. Result: NOT CLEAN pre-action — 1×P0, 4×P2,
4×P3, all actioned in-session (commits below). Verified clean by the leg: all six test
constants match their implementation templates character-for-character; trigger roster ↔ YAML
exact; action indices hold; five of six interleavings walk correctly.

| id | sev | finding (compressed) | resolution |
|---|---|---|---|
| R2R-1 | **P0** | `manual_detected` branches emit bare `true`/`false` TEXT → HA `literal_eval` fallback leaves a truthy STRING `'false'` → hold branch fires every tick, comfort control never runs; helper-unset instances crash on `set_value` with empty entity; the rendered test PINNED the defect (lowercase) | All boolean branches now `{{ true }}`/`{{ false }}` (→ real bools); constant + rendered expectations flipped to `"True"/"False"`; boolean-template rule added to plan Global Constraints |
| R2R-2 | P2 | Same bug class latent in v1.0.0 `door_is_open` no-sensor branch — masked only because both live instances configure door sensors; now load-bearing in a fix-wave-owned condition | Task 6 fixes the branch to `{{ false }}` + structural pin + rendered pin (`sensor_door=[]` → `"False"`) |
| R2R-3 | P2 | Spec claims STEP-2c *placement* prevents restart-reads-as-manual; actually STEP-1 vars are frozen pre-seed — the branch-6/7 `trigger.id != 'init'` guards are the real mechanism (spec-as-authority would delete them) | Spec rule 5 rewritten to name the guards as load-bearing |
| R2R-4 | P2 | Last-gap manual tweak (≤1 loop interval before expiry) starts a fresh full window at the expiry tick — can chain | Documented as accepted residual in rule 4 (per-tweak bounded; latest human input honored) |
| R2R-6 | P2 | In-range deadband-active sub-branch had no shape/guard pin — leftover v1.0 sequence-condition would abort pre-write → spurious hold every off-in-range tick, green suite | Dedicated positional pin `test_in_range_deadband_off_subbranch_shape` added (Task 8 §3.6) |
| R2R-5 | P3 | Round-half epoch can overshoot the deadline → non-deterministic 70-min hold from a 60-min setting | Floor (`\| int`) everywhere; anti-`round(` assert |
| R2R-7 | P3 | Fan-stub `max == high` makes stage-1/stage-2 arms indistinguishable | Distinct `turbo` + stage-1 case added |
| R2R-8 | P3 | `turn_off` calls lack `continue_on_error` symmetry — a pierce could fail to end a hold for one tick | Added to all four off-paths + pin + degradation row generalized |
| R2R-9 | P3 | `hold_active` bounded check had zero behavioral coverage (no `as_timestamp` stub); minutes-vs-seconds slip would kill the feature green | `as_timestamp` stub + 4-point rendered bounds test |

## Verdict

- BOARD_VERDICT: **PASS** (round cap 2 reached; every finding across both rounds actioned
  in-session; the code-time board independently re-verifies the implementation)
- OPEN_P0: 0 | OPEN_P1: 0
- DEGRADED: yes — ℹ architecture lens (Gemini R3) absent, standing; required quorum
  (Claude + Codex) met in full
- REPORT: reviews/board-20260809-110950.md

