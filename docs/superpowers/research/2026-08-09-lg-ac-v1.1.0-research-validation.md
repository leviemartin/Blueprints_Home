# Research Validation — LG AC Climate v1.1.0 (Quick)

**Date:** 2026-08-09 · **Verdict: PROCEED** — all five design-gating unknowns confirmed; no
design change forced. Dispatch: one Sonnet research agent, T1-source-disciplined (HA core
source + official docs), per Stack B research-validate Quick scope.

Related: beep-disable research (same day, in-session) — LG's own API client
(`thinq-connect/pythinqconnect`) has zero sound/buzzer resources; wall-mount manuals
(2021/2022/2024) document no mute (window-unit line only); maintainer + LG-Korea contact
confirm hardware disassembly as the only true disable. Command-frequency reduction (spec §1)
is the only software lever. Key refs: `ollo69/ha-smartthinq-sensors` issues #182, #831, PR #584.

## Verdicts

| # | Question | Verdict | Constraint locked into design |
|---|---|---|---|
| 1 | `states[x].last_reported` in templates | ✅ since HA 2024.4; updates on unchanged-value re-reports (unlike `last_updated`) | Use unconditionally; §5 contingency deleted |
| 2 | `for: {minutes: !input n}` on state trigger | ✅ `cv.positive_time_period_template` → `vol.Coerce(float)` accepts int/str | Door trigger MUST stay `platform: state` (pattern breaks on `device` triggers) |
| 3 | State trigger with `entity_id: []` | ✅ valid, silently never fires (`async_track_state_change_event` with `[]` registers no listeners) | ONLY because `door_sensor` is `multiple: true`; a scalar selector defaulting `[]` fails validation ("Entity [] is neither…") |
| 4 | `input_text` cap + `from_json` | ✅ 255-char hard cap (state machine, applies to `set_value`); bare `from_json` throws on bad input | Helper parse always `| from_json(default={})`; JSON doc ≈80 chars |
| 5 | `persistent_notification.dismiss` on absent id | ✅ no-op in practice — weakest-sourced (T3, not officially documented) | Add `continue_on_error: true` on dismiss steps |

## Citations

1. `State.last_reported` added: HA core-2024.4 changelog (PR #113511) —
   https://www.home-assistant.io/changelogs/core-2024.4/ · state-object docs ("updated
   regardless of any changes") — https://www.home-assistant.io/docs/configuration/state_object/ ·
   dev blog 2024-03-20 — https://developers.home-assistant.io/blog/2024/03/20/state_reported_timestamp/
2. `time_period_dict` coercion in `homeassistant/helpers/config_validation.py` —
   https://github.com/home-assistant/core/blob/dev/homeassistant/helpers/config_validation.py ·
   community confirmation (state vs device trigger caveat) —
   https://community.home-assistant.io/t/how-to-write-a-for-time-entry-with-an-input-parameter-in-a-blueprint/528307
3. `_entity_ids` passes lists through; empty list registers no listeners
   (`homeassistant/components/homeassistant/triggers/state.py`) —
   https://github.com/home-assistant/core/blob/dev/homeassistant/components/homeassistant/triggers/state.py ·
   scalar-mismatch failure mode + community optional-trigger patterns —
   https://community.home-assistant.io/t/use-optional-input-variable-as-a-trigger-entity/389995
4. 255-char cap — https://www.home-assistant.io/integrations/input_text/ · `from_json(default=…)`
   — https://www.home-assistant.io/template-functions/from_json/
5. Dismiss action docs (no missing-id statement; T3) —
   https://www.home-assistant.io/actions/persistent_notification.dismiss/
