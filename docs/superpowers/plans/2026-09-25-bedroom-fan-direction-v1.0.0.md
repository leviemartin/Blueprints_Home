# Bedroom Fan Direction v1.0.0 — kids-room season advisory (Session #45, epic #44) — revision under A1

```text
Driver: claude
Entry: A
Stakes: high-risk (held from wave 1; stakes never fall)
Trigger: deletion of a live fan automation S (blind stop/restart, the R2-03 lineage risk); a toggle write that S used to act on; an internet forecast in the decision; a push to a phone through a templated notify service. Physical-safety status: X sends NO fan command of any kind (A1 item 3); the only fan-affecting acts in this chain are the deletion of S and its disabled-on-load rollback.
Amendment: A1 advisory-only — /home/martin/AI/reviews/fan-direction-20260925/discovery-phases/amendment-a1-advisory-only.md (sha 176928b6; phase d2-kids-fan-direction-advisory; supersedes Stage B and every fan write in the brief)
Dispatch ledger: /home/martin/AI/reviews/fan-direction-20260925/ledger.json
Dispatch allowance: 24
Review allowance: 12
TCB baseline: /home/martin/AI/reviews/tcb-baseline-fan-direction-20260925.txt
TCB aggregate: 0a93e62d511a30b295befd7d06dea361cb5e87982d30ccaae1e0e984340ce8ad
TCB_EXTRA: /home/martin/AI/projects/Blueprints_Home/scripts/deploy-blueprint.sh
Declared TCB changes: none
Worktree: /home/martin/AI/projects/Blueprints_Home-wt/fandir, branch feat/kids-fan-direction off origin/main 688339d
Plan path: docs/superpowers/plans/2026-09-25-bedroom-fan-direction-v1.0.0.md
Boundaries:
  B5 X–old seasonal S at deploy: both react to the toggle; S answers a toggle change with a fan write (stop/restart if the fan is on). X's 18:00 toggle write must therefore never reach a live S: deploy order is delete S (Martin's go), confirm absent, then create X; rollback restores S only disabled-on-load and only on a separate go. This is the one physical-safety path left in the chain.
  B6 trust boundary: weather.home_sm (Met.no, internet) and sensor.openweathermap_temperature (internet) are read as validated numbers only (finite, −40…45 °C, unit °C, fresh, enough distinct forecast timestamps); a bad forecast can at worst flip the toggle once per evening and produce a wrong advisory, never a fan command; notice bodies are fixed wording with validated numbers and enum words only, no forecast strings.
  B7 authorization/deletion: every live write at deploy (delete S, blueprint save, instance create, forced-check config, real config, rollback) needs Martin's go; S is restored only with `initial_state: false` in the POSTed body and only on its own go; the ceiling-fan-hue-blueprint README note stays uncommitted unless Martin says commit.
  B8 toggle writer concurrency (Stage A vs Martin's hand flips): Stage A writes the toggle at most once per evening, only at its four ticks, only when the toggle's last_changed is older than today's decide_time (R2-05 latch); a hand flip after decide_time freezes Stage A for that evening; a hand flip before it may be overridden at decide_time when the night low is outside the band; writes are idempotent; there is no toggle trigger, so a flip never re-enters X.
  B9 notify-service input: `notify_services` is Martin's config text; each entry is admitted only if it matches `^notify[.][a-z0-9_]+$`; the templated `service: "{{ repeat.item }}"` exists only inside the one `repeat` over that filtered list with `continue_on_error: true`, and the persistent notice is created before the push loop, so a missing service loses nothing.
  Void under A1: B1 (cutoff), B2 (pre-cool), B3 (dimmer/manual starts), B4 (daytime) — X has no fan write, so no concurrency with any fan writer exists; pre-cool's 19:29 write and the cutoff's resume keep whatever direction the fan has.
Plan author: Claude driver: discovery-model subagent (Fable 5.1, xhigh) revision under A1, accepted by the orchestrator (Opus 5.5, high)
```

Sources: approved brief `/home/martin/AI/reviews/fan-direction-20260925/discovery-phases/d2-kids-fan-direction-advisory-resolved-brief.md` (sha 1987127c) as amended by A1; research-validate `/home/martin/AI/reviews/fan-direction-20260925/research/research-validate.md` (Stage A ships, defaults 8/14 °C, E8 raise-or-empty as one failure); overlap audit `/home/martin/AI/reviews/fan-direction-20260925/overlap/overlap-audit.md`. Unresolved product choices: none. The wave-1 plan is preserved at `/home/martin/AI/reviews/fan-direction-20260925/plan-wave1-corrected.md`.

## Pre-gate consistency check — status under A1

C1 (retry memory) and C2 (daily summary at apply_end) are VOID: no fan write, no verify, no apply window. C3 stands: `fan`, `season_toggle`, `weather_entity` required, every tunable defaulted. The B2 tightening (apply_end 19:00) is VOID.

## Design board wave 1 — status

The wave-1 correction record (enumerated paths 1–6 of "a set_direction reaches a spinning fan") is VOID as a class: X issues no direction command. Path 6 (S) survives as boundary B5. Martin's R2-01 device-check conditional is moot; A1 replaced it with advisory only.

## Wave 2 correction record (all wave-1 and delta-1 findings)

Initial R1: R1-01 VOID (no wait/delay; every run is instantaneous; no summary tick). R1-02 VOID for the guard; the tick definition is FIXED under R1-D1-08. R1-03 VOID (no wait_template; the structure test forbids wait_template and delay). R1-04 FIXED: step-kind allowlist {service/action, choose, variables, repeat} with `repeat` admitted only in the notify shape; forbidden-key walk scoped per R1-D1-01. R1-05 FIXED in its new shape: the advice notice is dismissed on any quarter-hour tick that finds the direction matching (the verify/pending notices no longer exist). R1-06 FIXED: a scripted read-only `trace/get` of the first Stage A run between 18:01 and 18:40 each observation evening, plus surviving evidence (toggle history, push received, forecast notice absent). R1-07 FIXED: criterion "toggle writes by X only at the four Stage A ticks, at most one per evening, never inside the band". R1-08 VOID. R1-09 FIXED: `is defined` guards in `is_tick`, pinned.
Initial R2: R2-01 VOID, R2-02 VOID (no direction command). R2-03 FIXED: rollback = X off with stop_actions, X deleted, then only on a separate go S restored from a file that carries `initial_state: false`, confirmed `off` before anything else; stop on any failed prerequisite. R2-04 FIXED incl. delta: outdoor reading finite/−40…45/°C/< 2 h; weather entity available, `temperature_unit` °C, last_updated ≤ 3 h before either forecast is eligible; hourly coverage counted over distinct in-window timestamps (≥ 10) with the earliest ≤ night_start + 90 min and the latest ≥ night_end − 90 min; tests for °F, stale, duplicate timestamps, clustered coverage, NaN/inf/string. R2-05 FIXED: last_changed latch (restart-safe on the safe side: a restart after decide_time freezes that evening) with the R1-D1-07 absent-toggle guard. R2-06 VOID. R2-07 VOID for the write; the advisory treats a direction outside {forward, reverse} as "unknown" by construction.
Delta R1: R1-D1-01 FIXED (forbidden keys checked on step-mapping and choose-option keys only, never inside data/target; positive expectation that both get_forecasts steps carry `data.type` in {hourly, daily}). R1-D1-02 VOID (no delay). R1-D1-03 VOID. R1-D1-04 FIXED by this rewrite. R1-D1-05 VOID (no E4/2b steps). R1-D1-06 FIXED (example 10 reworded to drop-and-count). R1-D1-07 FIXED (guarded `toggle_changed_ts`). R1-D1-08 FIXED (one `stage_a_ticks` list drives Stage A; `decide_time` stays free; rendered test with 18:10).
Delta R2 (leg schema-invalid; content treated as evidence): R2-01, R2-02, R2-D1-01, R2-D1-02, R2-07 VOID (no fan command is ever submitted, so no delayed or queued delivery exists). R2-03 delta FIXED as above (no outstanding device commands can exist because X never sends any). R2-04 delta FIXED as above. R2-D1-03 FIXED with R1-D1-08.

## Behaviour contract — bedroom_fan_direction.yaml v1.0.0

**Inputs** (all through top-level `variables:` as `<name>: !input <name>`): `fan` (fan, required — read only); `season_toggle` (input_boolean, required; off = summer, on = winter); `weather_entity` (weather, required); `outdoor_sensor` (sensor, multiple, default `[]`, first entry used); `notify_services` (text, multiple, default `[]`); `fan_name` (text, default `the fan`); `summer_direction` `forward`, `winter_direction` `reverse` (select forward/reverse); `outdoor_cold` 8.0 (−10…20, step 0.5 °C); `outdoor_warm` 14.0 (0…30, step 0.5; must exceed `outdoor_cold`); `decide_time` 18:00:00; `night_start` 19:30:00; `night_end` 07:15:00. No interlock, window, verify or retry inputs.

**Triggers:** `time_pattern` minutes `/1` id `tick`; `homeassistant` start id `ha_start` (evaluate only). No toggle trigger, no global condition. `mode: single`, `max_exceeded: silent`.

**Predicates** (action-level `variables:` steps rendered key by key in file order; instants as unix timestamps; only strings, numbers, booleans and lists cross a step):

- Step 1: `is_tick = trigger is defined and trigger.id is defined and trigger.id == 'tick'` (R1-09; ha_start and manual Run issue nothing); `now_ts`; `decide_start_ts = as_timestamp(today_at(decide_time))`; `stage_a_ticks = [decide_start_ts, +900, +1800, +2700]`; `stage_a_index` = the k with `stage_a_ticks[k] <= now_ts < stage_a_ticks[k] + 60`, else −1; `stage_a_due = is_tick and stage_a_index >= 0`; `first_tick = stage_a_due and stage_a_index == 0`; `quarter_tick = is_tick and now().minute % 15 == 0`; `night_start_ts`, `night_end_ts` (tomorrow); `weather_ok` = entity in the state machine, state not unavailable/unknown, `temperature_unit` attribute `°C`, `last_updated` ≤ 3 h old (R2-04 delta); `weather_daily_supported` (bit 1, pre-cool idiom); `notify_list` = `notify_services` normalised to a list and filtered by `^notify[.][a-z0-9_]+$` (heating-rack idiom); `fan_name_safe` = `fan_name` if it matches `^[A-Za-z0-9 _-]{1,32}$` else `the fan`.
- Step 2: `choose` on `stage_a_due and weather_ok` → `weather.get_forecasts` hourly, `continue_on_error: true`, `response_variable: hourly_resp`.
- Step 3: `hourly_list_safe` (pre-cool's guarded extraction; raise, missing key, scalar or empty list give `[]`); `night_low_hourly` = min `temperature` over entries with a parsable datetime inside [night_start_ts, night_end_ts] and a finite temperature within −40…45, dropping other entries, and only when the distinct in-window timestamps number ≥ 10, the earliest ≤ night_start_ts + 5400 and the latest ≥ night_end_ts − 5400; else `none`; `daily_due = stage_a_due and weather_ok and night_low_hourly is none and weather_daily_supported`.
- Step 4: `choose` on `daily_due` → `weather.get_forecasts` daily → `daily_resp`.
- Step 5: `daily_list_safe`; `night_low_daily` = `templow` of the entry whose local date is tomorrow when finite within −40…45, else `none`; `outdoor_now` = first `outdoor_sensor` state as float when finite, within −40…45, unit `°C`, `last_updated` < 2 h, else `none`; `night_low`, `night_low_source` by precedence hourly → daily → live → `none`; `toggle_state`, `toggle_known = toggle_state in ['on','off']`; `toggle_changed_ts = as_timestamp(states[season_toggle].last_changed) if states[season_toggle] is not none else 0` (R1-D1-07); `season_target` = winter if `night_low <= outdoor_cold`, else summer if `night_low >= outdoor_warm`, else hold (`none` → hold); `toggle_write_due = stage_a_due and toggle_known and season_target != 'hold' and (season_target == 'winter') != (toggle_state == 'on') and toggle_changed_ts < decide_start_ts` (R2-05); `forecast_degraded = stage_a_due and night_low_source in ['live','none']`.
- Step 6 (Stage A actions): `input_boolean.turn_on` / `turn_off` on `{{ season_toggle }}` under `toggle_write_due`; under `stage_a_due`: `persistent_notification.create` id `bedroom_fan_direction_forecast_<this.entity_id>` if `forecast_degraded` (fixed text naming the source used; for `none`, that no season decision was made), else `dismiss` of that id. Persistent only, no push.
- Step 7 (read after the write): `toggle_now` (live state), `toggle_now_known`; `toggle_changed_now = toggle_write_due and toggle_now != toggle_state`; `season` = winter/summer/''; `target_direction` = `winter_direction` or `summer_direction` when in ['forward','reverse'], else ''; `fan_state` (`absent` when missing); `fan_available = fan_state not in ['unavailable','unknown','absent']`; `fan_direction` = the attribute when in ['forward','reverse'], else ''; `direction_ok = toggle_now_known and target_direction != '' and fan_direction != '' and fan_direction == target_direction`; `advice_reason` = `ok` if direction_ok, else `fan_unavailable` if not fan_available, else `direction_unknown` if `fan_direction == ''`, else `differs`; '' when the toggle or target is unknown; `advice_due = (first_tick or (stage_a_due and toggle_changed_now)) and advice_reason in ['differs','direction_unknown','fan_unavailable']` (A1 item 4: at the first tick, and again on a later tick only if it changed the toggle; the latch makes ≤ 2 per evening structural); `dismiss_due = quarter_tick and direction_ok`; `night_low_known = night_low is not none`; `fan_state_word` = fan_state when in [on, off, unavailable, unknown, absent] else unknown; `fan_direction_word` = fan_direction or unknown.
- Step 8: one `choose`. Option 1 `advice_due`: `persistent_notification.create` id `bedroom_fan_direction_advice_<this.entity_id>`, title "Bedroom fan direction", message = prefix "Night low {{ night_low | round(1) }} °C" (or "Night low unknown") followed by, per reason — differs: ": set {{ fan_name_safe }} to {{ target_direction }} ({{ season }}) while it is off. It now reads {{ fan_direction_word }}."; direction_unknown: ": {{ fan_name_safe }} reports no direction (state {{ fan_state_word }}). Check it is set to {{ target_direction }} ({{ season }}) while it is off."; fan_unavailable: ": {{ fan_name_safe }} is {{ fan_state_word }}; its direction cannot be checked. It should be {{ target_direction }} ({{ season }}) tonight."; then `repeat: for_each: "{{ notify_list }}"` → `service: "{{ repeat.item }}"`, `continue_on_error: true`, same title and message. Option 2 `dismiss_due`: `persistent_notification.dismiss` of the advice id.

**Instance** `deploy/bedroom_fan_direction_kids.json`: id `bedroom_fan_direction_kids`, alias "Kids room — fan direction v1.0.0", `use_blueprint.path` `leviemartin/bedroom_fan_direction.yaml`, inputs `fan: fan.ceiling_fan_light_v2`, `season_toggle: input_boolean.samuel_fan_winter_mode`, `weather_entity: weather.home_sm`, `outdoor_sensor: [sensor.openweathermap_temperature]`, `notify_services: [notify.mobile_app_martin_fold]`, `fan_name: kids fan`, defaults for the rest, `trace.stored_traces` 60. Retired S: `deploy/samuel_fan_seasonal_direction.retired.json` (byte copy of `overlap/cfg/automation.samuel_fan_seasonal_direction.json`) and `deploy/samuel_fan_seasonal_direction.restore-disabled.json` (the same object plus `"initial_state": false`).

## Invariants pinned by structure tests (tests/test_bedroom_fan_direction_structure.py)

1. Name "Bedroom Fan Direction v1.0.0", `**Version: 1.0.0**`, domain automation; inputs exactly the set above with the listed defaults; required {fan, season_toggle, weather_entity}; `outdoor_warm` default > `outdoor_cold` default; every input through top-level `variables:`.
2. `mode: single`, `max_exceeded: silent`; trigger ids `[tick, ha_start]`, no `state` trigger, no global condition; `is_tick` contains `trigger is defined and trigger.id is defined`.
3. Service allowlist over the parsed tree: every literal `service`/`action` value ∈ {`input_boolean.turn_on`, `input_boolean.turn_off`, `weather.get_forecasts`, `persistent_notification.create`, `persistent_notification.dismiss`}; exactly one templated value, `{{ repeat.item }}`, and it sits inside the only `repeat`, whose `for_each` is `{{ notify_list }}` and whose sequence is that single step with `continue_on_error: true`; the `notify_list` template contains the literal `^notify[.][a-z0-9_]+$`; no literal `notify.` service. Raw text: zero matches for `fan\.(turn_on|turn_off|toggle|set_[a-z_]+|increase_speed|decrease_speed|oscillate)`, `set_direction`, `set_percentage`, `homeassistant\.`, `script\.`, `automation\.`, `device_id`, `scene:`, `event:`. The deploy-consistency test extends its banned-service scan to `bedroom_fan_direction.yaml`.
4. Both `input_boolean` steps target `{{ season_toggle }}` under `toggle_write_due`; both `get_forecasts` steps carry `continue_on_error: true`, a `response_variable` and `data.type` ∈ {hourly, daily}; the advice option's condition is `advice_due`, its first step is the persistent create and its second the repeat.
5. Step-kind allowlist: every action node (top level, choose options, repeat sequence) is one of service/action, choose, variables, repeat; the keys `wait_template`, `delay`, `wait_for_trigger`, `stop`, `if`, `parallel`, `condition`, `event`, `scene`, `device_id`, `type` are absent from step mappings and choose-option mappings (never descending into `data:`/`target:`).
6. Instants: no `.strftime(`, no `_dt` variables; `last_updated` appears only in the outdoor-freshness and weather-freshness keys; `trigger.to_state`/`trigger.from_state` absent; predicate names in the order above, each using only earlier names, inputs, `trigger`, `this`, `repeat` and HA globals; every rendered key is a bool, number, short string or list.
7. Notification ids: two fixed stems (`forecast`, `advice`), each templated on `this.entity_id`.
8. Message hygiene (B6): every `title`/`message` template in the tree references only {night_low, night_low_known, season, target_direction, fan_direction_word, fan_state_word, fan_name_safe, night_low_source} (`meta.find_undeclared_variables`), never a response variable or list; the push data equals the persistent data.

Test harness: extend the daytime harness (strict fakes, per-key `_reparse`, `_calls` interpreter) so that (a) every action-level `variables:` step renders at its position against the live world, (b) a `weather.get_forecasts` step injects a fake response per type (dict, `forecast: []`, or a raise leaving the variable undefined), (c) an `input_boolean` call updates the fake toggle before the next step, (d) `repeat.for_each` iterates the rendered list with `repeat.item` bound and records each templated service call. Expectations come from this contract, never from the YAML.

## Acceptance examples → tests (rendered; Europe/Amsterdam)

1. 18:00 tick, hourly night low 4 °C, toggle off, fan off forward: calls = [input_boolean.turn_on, dismiss(forecast), create(advice), notify.mobile_app_martin_fold]; the message contains "4.0 °C", "reverse", "winter", "forward"; no other call. Same world with the fan already reverse: [turn_on, dismiss(forecast), dismiss(advice)] — no push. → ex. 1
2. Direction matches at 18:00: [dismiss(forecast), dismiss(advice)]; at 12:15, 20:30: [dismiss(advice)]; at 12:01: []. → ex. 2
3. Night low 10 °C, toggle on, fan forward: no input_boolean call; create(advice) + push "reverse (winter)". → ex. 3
4. Night low 5 °C, toggle on: no input_boolean call. → ex. 4
5. Lows 16, 17, 15 °C, toggle off: no writes; band 8/14 pinned. → ex. 5
6. Fan on at 18:00 with a differing direction: toggle written, advice sent, no fan call; nothing at 19:00. → ex. 6
7. ≤ 2 per evening: lows 4/16/4/16 at the four ticks, toggle off since yesterday: exactly one turn_on (18:00) and one advice (18:00), nothing at 18:15–18:45 (latch); toggle hand-flipped at 18:05 → no Stage A write and no push that evening; 18:00 no change + 18:15 band edge change → advice at 18:00 and at 18:15, none later. → A1 item 4, R2-05
8. Fan unavailable/unknown/absent at 18:00: one advice with reason fan_unavailable (message names the state); fan off with no direction attribute: reason direction_unknown; 18:15 with the fan back and matching: [dismiss(advice)]. → ex. 10
9. `sensor.temperature_sensor_3`, `binary_sensor.*fanprotection` absent from inputs and text: never read. → ex. 11
10. Forecast at 18:00: ≥ 10 distinct in-window timestamps → hourly min; an entry that is NaN, inf, outside −40…45 or a string is dropped and hourly stays valid while ≥ 10 valid distinct timestamps remain (12 entries with one NaN → min of 11; with three invalid → daily) (R1-D1-06); 12 rows sharing 3 timestamps → daily; 10 rows all in 20:00–01:00 → daily (endpoint rule); weather `temperature_unit` °F, weather unavailable or last_updated 4 h → both forecasts absent → live; outdoor stale 3 h / °F / NaN → absent; hourly raise or `[]` → tomorrow's daily templow (not today's row); all absent → no input_boolean call and the forecast notice; message bodies contain no forecast strings (a `condition: "<script>"` field never appears). → ex. 12, E8, R2-04
11. Hand flip to on at 10:00 (tick runs only): [] at 10:00, 10:01, 10:15 (direction not ok, no dismiss); flip at 21:00 → nothing until next day's 18:00, where the latch permits a write. → ex. 13
12. `ha_start`, manual Run: no service call in any world; toggle absent from the state machine: no call, no render error. → ex. 14, R1-D1-07
13. Flip to off at 17:00, 18:00 low 4 °C → turn_on; low 11 °C → hold, advice if mismatched. → ex. 15
14. `decide_time: 18:10:00` → Stage A at 18:10/18:25/18:40/18:55 only, `first_tick` at 18:10; `quarter_tick` dismissal unaffected. → R1-D1-08
15. `notify_services` filter: `[]` → no push step; `mobile_app_x`, `notify.a b`, `fan.turn_on`, `notify.x; fan.turn_on` dropped; `notify.mobile_app_martin_fold` kept; a string input becomes a one-item list. `fan_name` "kids fan" kept; "<b>x</b>" → "the fan". → B9
16. Consistency test (`tests/test_bedroom_fans_deploy_consistency.py`): kids instance fan = the live cutoff's fan, toggle = `input_boolean.samuel_fan_winter_mode`, notify entry matches the regex; `.retired.json` id `samuel_fan_seasonal_direction` with the same fan and toggle; `.restore-disabled.json` == retired + exactly `initial_state: false`. → ex. 16, R2-03
17. Stage A present (E1 proceed), pinned. → ex. 17

Plus: `outdoor_warm <= outdoor_cold` never yields both targets (cold first); Stage A never on non-tick runs; dry-run of the instance passes.

## Tasks

Shared invariants for every worker brief: owned paths only; no edits to `scripts/`, other blueprints or their tests; no `git stash`; no commit or push; no nested dispatch, research or review; return unresolved items to the orchestrator; run `cd <worktree> && ~/projects/ceiling-fan-hue-blueprint/.venv/bin/python -m pytest tests -q` before returning. At most two delegates active at once; T3 and T4 are sequential in the same worktree.

- **T1 — pre-gate consistency check.** Done (Sonnet/high); status recorded above.
- **T2 — stage 4 design gate, wave 2 (final correction wave).** Orchestrator: `convene-board` delta review of this revision with `STACKB_DRIVER=claude`: R1 Fable/xhigh (`r1-model: fable`, correctness with B5–B9 in the charter) and R2 fresh Codex dedicated security (B6 injection surface incl. the templated notify service, B7 deletion/rollback, B9). No third wave.
- **T3 — stage 5 blueprint + tests + instance.** Route delegate; Opus/high (high-risk chain, physical-safety lineage never routes down); worktree `Blueprints_Home-wt/fandir`. Owns `bedroom_fan_direction.yaml`, `tests/test_bedroom_fan_direction_structure.py`, `deploy/bedroom_fan_direction_kids.json`, `deploy/samuel_fan_seasonal_direction.retired.json`, `deploy/samuel_fan_seasonal_direction.restore-disabled.json`, the direction additions to `tests/test_bedroom_fans_deploy_consistency.py`. TDD: tests RED against an empty file first. Patterns: `bedroom_fan_daytime.yaml` (predicate style), `bedroom_precool.yaml` STEP 2b (forecast idiom), `bathroom_heating_rack.yaml` `notify_list` and its `repeat` push, `tests/test_bedroom_fan_daytime_structure.py` and `tests/test_bathroom_heating_rack_structure.py` (harness, notify filter tests). Acceptance: full suite green; `bash scripts/deploy-blueprint.sh --dry-run bedroom_fan_direction.yaml leviemartin/bedroom_fan_direction.yaml deploy/bedroom_fan_direction_kids.json` passes; `grep -cE "fan\.(turn_on|turn_off|toggle|set_[a-z_]+)|set_direction" bedroom_fan_direction.yaml` = 0; `git status --porcelain` lists only owned paths. Depends on T2 PASS.
- **T4 — stage 5 docs.** Route delegate; Sonnet/high (bounded prose; pattern `requirements_bedroom_fan_daytime.md`, README daytime section). After T3 returns. Owns `requirements_bedroom_fan_direction.md` (contract, actors table, A1 statement "sends no fan command", boundaries B5–B9, deploy and rollback), README section "Bedroom Fan Direction Blueprint", one-line updates in `requirements_bedroom_fan_daytime.md` (actor S removed) and the README daytime overview. Acceptance: suite green; every input, notice id and example number matches the YAML and tests (orchestrator diff-checks).
- **T5 — stage 6 code gate.** Orchestrator: `convene-board` code board, R1 fresh Opus/high correctness + R2 fresh Codex security, delta verification per wave; keep 4 review slots for one full delta set before any optional dispatch.
- **T6 — stage 7 checks.** Command: full suite; dry-run; `bash -n scripts/deploy-blueprint.sh`; TCB verify against the baseline (script hash unchanged); `git diff --stat origin/main` names only owned paths; PR body `Session: #45`, secret-scanned, via `github-sync`; commit and push only on Martin's go.
- **T7 — stage 8 deploy runbook** (orchestrator, command steps, from `/home/martin/AI/projects/Blueprints_Home` after merge; Martin's go before each numbered live write; between 12:00 and 16:00 (4b's decide_time must lie within the F10 range 12:00–21:00) so the forced check runs before the real 18:00 evaluation; token via the script's header-file idiom, never argv). No live fan write anywhere in this runbook.
  0. Read-only preconditions: fan off, toggle off, `weather.home_sm` available; live `GET /api/config/automation/config/samuel_fan_seasonal_direction` byte-equal to `.retired.json` (else stop); `GET /api/services` lists `notify` → `mobile_app_martin_fold`.
  1. GO: `DELETE /api/config/automation/config/samuel_fan_seasonal_direction`; confirm `automation.samuel_fan_seasonal_direction` absent from `/api/states`.
  2. GO: `bash scripts/deploy-blueprint.sh bedroom_fan_direction.yaml leviemartin/bedroom_fan_direction.yaml` (blueprint save only, no instance argument).
  3. GO: `POST /api/config/automation/config/bedroom_fan_direction_kids` with the real instance file; confirm `state=on` and `attributes.id`; after 2 min the tick traces show no service call.
  4. Forced check. 4a (Martin, no go needed): flip the toggle ON by hand with the fan off reading forward; for 2 min the tick traces show no service call and no push arrives. 4b GO: build `<scratch>/forced-check.json` from the real file with `jq '.use_blueprint.input += {"decide_time": "<next quarter-hour ≥ 5 min ahead>", "outdoor_cold": -10, "outdoor_warm": 30}'` and POST it; at that tick the trace shows [get_forecasts hourly, dismiss(forecast), create(advice), notify.mobile_app_martin_fold] with `night_low_source` hourly, no input_boolean call (hold band), no fan service; exactly one push on the phone reading "set kids fan to reverse (winter)". 4c GO: POST the real instance file again; Martin flips the toggle back OFF by hand (before 18:00, so the latch allows the evening write); the next quarter tick shows only dismiss(advice) and the persistent notice is gone. Any deviation → stop; S stays deleted; rollback only on Martin's go.
  5. Observe the 18:00 evaluation the same evening (first observation evening).
  R. Rollback (R2-03), each on Martin's go, stop on any failed prerequisite: (1) `automation.turn_off` X with `stop_actions: true`, confirm `off` and no running trace; (2) `DELETE /api/config/automation/config/bedroom_fan_direction_kids`, confirm absent; dismiss X's two persistent ids by hand; (3) only if Martin separately asks for S back: POST `deploy/samuel_fan_seasonal_direction.restore-disabled.json` (carries `initial_state: false`), then confirm `automation.samuel_fan_seasonal_direction` reads `off` before any other step; re-enabling S's blind stop/restart needs its own explicit go. Toggle untouched; no fan command in any rollback step.
- **T8 — stage 8 local README note.** Orchestrator inline (tiny-remainder exception): add to `~/projects/ceiling-fan-hue-blueprint/README.md` under "Seasonal direction" that the kids instance was deleted on the deploy date and replaced by Blueprints_Home `bedroom_fan_direction.yaml` v1.0.0 (advisory only; nothing in HA reverses the fan any more); file kept for reference; uncommitted unless Martin says commit.
- **T9 — stage 9 closing.** Session #45 carries `<!-- observe:open -->` at deploy; close only after the observation passes (`observe:closed`) with the closing record (dispatch outcomes, findings and dispositions incl. this wave-2 record, hashes, residual: the 8/14 °C band unvalidated on a local winter, the master-bedroom instance deferred).

## Stage 8 observation (≥ 3 evenings from the deploy evening) `<!-- observe:open -->`

Measured from logbook/history (context parent = `automation.kids_room_fan_direction_v1_0_0`) plus one scripted read-only `trace/get` of the first Stage A run per evening taken between 18:01 and 18:40 (R1-06; 60 traces ≈ 1 h). Pass when: each evening the decision is visible (`night_low_source` hourly on ≥ 2 of 3 evenings; the forecast notice absent otherwise explains itself); toggle writes by X only at 18:00/15/30/45, at most one per evening, never inside the band (R1-07); a push arrives exactly when the direction differs at 18:00 (or at a later tick that changed the toggle) and never otherwise, ≤ 2 per evening; the persistent advice clears within 15 min of the direction matching; zero services outside the allowlist and zero `fan.*` in any X trace (A1 item 6); no fan state change in the logbook has X as its context parent; pre-cool's 19:29 write still lands (fan on, 1 %) unchanged. Carried item: 8/14 °C unvalidated on a local winter (E3); re-check the first evening the night low reads below 8 °C.

## Decided by orchestrator (proposed; Martin may revert)

1. Triggers are tick and ha_start only; the toggle state trigger is dropped because no action keys on a hand flip (A1: no push outside the 18:00 ticks).
2. `mode: single`, `max_exceeded: silent`; runs are instantaneous (no wait or delay), so queued adds nothing.
3. Push fan-out reuses the repo's `repeat: for_each` over the filtered `notify_list` (heating rack, ventilator), re-admitting `repeat` in that single pinned shape. Alternative: call only `notify_list[0]` with no repeat.
4. Advice dismissal is checked on quarter-hour ticks of every hour (≤ 96 dismiss calls a day, clears within 15 min). Alternative: every tick when the fan's `last_updated` is ≤ 120 s old (clears within 2 min, one more `last_updated` use).
5. All three advice reasons push (differs, direction unknown, fan unavailable), reading A1's "say so in the notice" literally. Alternative: persistent-only for the two unknown reasons (the audit shows occasional Tuya unavailability).
6. The forecast-degraded notice stays persistent-only.
7. `fan_name` text input (validated charset, default "the fan"; instance "kids fan") so the message reads as A1's example.
8. Freshness constants (weather 3 h, outdoor 2 h) and the coverage rule (≥ 10 distinct timestamps, endpoints within 90 min) are fixed constants, not inputs.
9. The forced live check uses a hold-band test config (cold −10 / warm 30, `decide_time` at the next quarter) so it is deterministic in any weather and X writes nothing but notifications; the toggle-write path is proven by the rendered tests and the first real 18:00.
10. Two retired-S files: `.retired.json` (byte copy for the byte-equal precondition) and `.restore-disabled.json` (plus `initial_state: false`), both pinned by the consistency test.
11. In the advice branch the persistent create precedes the push loop, so a missing notify service (`continue_on_error`) never loses the notice.
12. Instance id `bedroom_fan_direction_kids`, alias "Kids room — fan direction v1.0.0", `stored_traces` 60; cutoff, daytime, pre-cool instances untouched; docs task on Sonnet/high, all YAML and test work on Opus/high.
13. C3: entity inputs `fan`, `season_toggle`, `weather_entity` required; every tunable defaulted (daytime pattern).
14. `summer_direction`/`winter_direction` stay as select inputs (a deferred master instance may map differently); values outside {forward, reverse} render as unknown target and suppress the advice.
15. `deploy/bedroom_fan_direction_kids.prev.json` is written by `deploy-blueprint.sh` on any redeploy; not owned by a task.

## Post-wave-2 fold-ins (binding; they override any conflicting line above)

Design gate closed ACCEPTED-RISK (Martin, 2026-09-25) on R2-D2-01 with live evidence (S `last_triggered: null`, `current: 0` at 2026-09-25 ~17:25Z) plus fix F1; R2 re-verifies F1 at the code gate. All wave-2 P2/P3 are folded in here.

- **F1 (R2-D2-01, P1 accepted-risk + fix) — S drain before deletion.** T7 step 0 adds: read `automation.samuel_fan_seasonal_direction` attributes; require `current == 0` and `last_triggered` null or older than the 2026-09-25 audit. New step 0b (GO): `automation.turn_off` S with `stop_actions: true`; confirm state `off`, `current == 0`, `last_triggered` unchanged. Step 1 deletes S only after 0b passes; any deviation (S ran since the audit, or is running) → stop and report; do not delete, do not create X.
- **F2 (R2-D2-02, P2) — explicit toggle-write suppression for the forced check.** New boolean input `write_toggle` (default `true`); `toggle_write_due` additionally requires `write_toggle`. T7 4b sets `write_toggle: false` (keep the hold-band thresholds as belt-and-braces); rendered test: `write_toggle: false` with night low 4 °C and toggle off → no input_boolean call, advice still evaluated.
- **F3 (R1-D2-01, P2) — response-variable scope.** Pre-declare `hourly_resp: {}` and `daily_resp: {}` in a top-level `variables:` step before each fetch `choose`, so the assignment inside the option updates the outer variable (HA ≥ 2025.4 block scoping). Structure test pins the pre-declarations; the harness models block scope (an undeclared response_variable is undefined after the branch) so the tests go red without them. Live check at deploy: the first 18:00 trace shows `night_low_source: hourly`.
- **F4 (R1-D2-02, P2) — summer path tests.** Add: toggle on since yesterday, fan reverse, 18:00 low 16 °C → [get_forecasts hourly, turn_off, dismiss(forecast), create(advice) containing "forward", "summer", "reverse", push]; same with fan forward → [get_forecasts hourly, turn_off, dismiss(forecast), dismiss(advice)]. Pin that the turn_on option's condition references `season_target == 'winter'` and the turn_off option's `season_target == 'summer'`.
- **F5 (R1-D2-03, P3).** The raw-text ban list applies to the `action:` block only (the trigger `event: start` and descriptions are outside it); `event`/`scene` stay banned as step keys via invariant 5.
- **F6 (R1-D2-04, P3).** B9/Decided 11 reworded: safety of a missing notify service comes from ordering (persistent create first; the repeat is the last step of the advice option and Step 8 the last action), since HA re-raises ServiceNotFound despite `continue_on_error`. Structure test pins both "last step" facts; the harness treats an unregistered service as fatal.
- **F7 (R1-D2-05, P3).** T7 step 3: "no service call other than `dismiss(advice)` at a :00/:15/:30/:45 tick".
- **F8 (R1-D2-06, P3).** The harness records `weather.get_forecasts` in `_calls`; call lists in examples 1, 2, 10 include it; assert no fetch at 12:01/12:15/10:00, none when `weather_ok` is false, no daily fetch when hourly is valid, daily only after hourly fails.
- **F9 (R1-D2-07, P3).** `outdoor_sensor` normalised like `notify_services` (string → one-item list), guarded with `| length > 0`; rendered cases `outdoor_sensor: []` (source none when forecasts also absent, no error) and a plain string value.
- **F10 (R1-D2-08, P3).** `decide_time` is valid only within 12:00–21:00 (documented in the input description); `decide_ok` guards Stage A (outside the range → no Stage A, one persistent forecast notice stating the misconfiguration); rendered case `decide_time: 23:50` → no Stage A.
